# 装机模拟器开发排期规划

## 当前目标

以 GodotVersion 为主线，保留当前可玩的购买、背包、安装、订单、评分、存档和 MCP 验证流程，继续对标 `D:\2work\2AKGStudio\1GameDev\1装机人生` 内的 PC Creator 2 资料做视觉与交互复刻，并把首页底部、订单、系统和任务中心统一成更清晰的工作区与全屏 overlay。

## 近期迭代目标

- 先统一首页、工作台、商店、背包、订单和模拟 OS 的视觉语言，继续清掉窄 tab、不可点背景和局促布局。
- 把模拟 OS 的软件配置完成度真正接入订单评分、交付结果和奖励，形成可见的任务奖励闭环。
- 把竞品素材、配件图和 UI 资源整理成可追踪的数据管线，减少脚本里的硬编码常量。
- 清理发布包里的开发工具、测试入口和未使用资源，保持 release 只带正式玩家需要的内容。

## 已完成基线

- Unity 原项目已归档到 `UnityVersion/`，Godot 主线位于 `GodotVersion/`。
- 已完成基础装机流程：购买、背包堆叠、安装/替换、兼容性检查、完成检测。
- 已完成订单池、接单、交付、奖励、交付评分。
- 已完成保存、读取、重开。
- 已完成新手自动流程脚本和 MCP UI smoke 验证。
- 已完成首页底部 `HomeBottomDock`、左侧 `WorkbenchFooter`、Task Center、Order Desk、Catalog overlay、模拟 OS、金手指和交付回执等核心界面。
- 已完成 R2 内容扩展：12 个订单、7 类客户、分阶段解锁、软件任务和 60 分钟首发内容基线。
- 竞品核心素材已归档到 `ReferenceAssets/PC_Creator2/`；`GodotVersion/assets/pc_creator2/` 只保留 `.gdignore` 和边界说明，不再存放竞品图片或 Godot 导入副本。

## 竞品复刻计划

### P0 当前阶段：视觉方向切换

- 将灰黑工具界面切换为 PC Creator 2 风格的深蓝紫手游界面。
- 工作台引用竞品房间/工作台图，替换旧占位网格图。
- 商店、背包增加配件详情图预览。
- 每次 UI 改动后必须跑截图检查和 MCP smoke。

验收：

- `verify_ui_screenshot.gd` 生成 1280x720 截图且无明显重叠。
- `mcp_ui_smoke.mjs` 能通过 MCP 找到 UI、点击按钮并读回状态。

### P1 商店/背包卡片化（已完成）

- [x] 将商店从文本列表改成配件卡片网格。
- [x] 每张卡片展示类型、名称、价格、等级、兼容/订单状态和配件图。
- [x] 背包同样卡片化，并保留拖拽安装、排序、出售。
- [x] 减少 tab 来回切换，保留“购买并安装”作为主路径。

验收：

- 1280x720 下商店至少展示一行 3 张以上卡片。
- MCP 点击购买/交付失败提示仍可读回。
- 旧 headless 回归全部通过。

### P2 装机区可视化插槽（进行中）

- [x] 左侧工作台从按钮列表升级为可视化插槽。
- [x] 安装后在机箱/工作台区域显示配件状态，而不是只显示文字按钮。
- [x] 机箱图上的插槽和下方配件卡都支持背包拖拽安装；拖错槽位会走失败反馈。
- [x] 工作台主区域加入当前槽位、完成度、开机测试、装机检查和下一步提示。
- [x] 720p 下将旧槽位按钮压缩为 5 列 2 行，避免主工作台被纵向裁切。

验收：

- `verify_workbench_overlay.gd` 覆盖点击机箱插槽筛选、已装配件右键菜单、卸载回背包、拖拽安装到机箱插槽、拖错槽位失败反馈，以及拖拽到下方视觉卡安装。
- `verify_workbench_screenshot.gd` 生成 1280x720、1366x768、1920x1080 工作台截图；紧凑化后的工作台与 footer 在三档分辨率下都通过，截图中已安装配件和底部槽位摘要都可见。
- MCP UI smoke 通过，识别到 193 个 UI 文本，确认引导可见且截图接口可用。

### P3 模拟系统深化

- 开机后进入更像手机/电脑系统的面板，软件配置完成度会影响订单评分和交付结果。
- 增加系统信息、跑分、故障提示、客户验收反馈。
- 交付评分可视化，接近竞品 `pc_score.png` 风格。

验收：

- 未按电源无法交付。
- 跑分、评分和软件配置完成度能在 UI 中清楚展示，并反映到订单结果。

### P4 素材与数据管线

- 整理竞品素材索引，标记可直接使用、需裁剪、需重绘、需替代的资源。
- 将配件类型与图片映射从脚本常量迁移到 JSON 配置。
- 建立后续 3D/2D 资源导入规则。

验收：

- 新增配件类型不需要改 UI 脚本即可绑定图片。
- 素材来源、用途和目标路径在文档中可追踪。

## 发布上线路线

后续 Codex 工作流以本节为主排期。用户说“下一步”或“继续”时，默认推进最靠前的未完成项；每一项都必须包含实现、自动化验证和必要的截图检查。

### R0 发布工程化（已完成）

- [x] 建立持续迭代目标和发布阶段。
- [x] 添加 Windows Desktop release 导出预设。
- [x] 添加一键发布脚本，串联导入、全部 headless 测试、导出、成品启动冒烟、ZIP 和 SHA-256 清单。
- [x] release 构建自动停用 MCP 运行桥，避免公开版保留调试 IPC。
- [x] 在本机实际产出第一份 Windows ZIP，并记录构建结果。

首份构建记录（2026-07-11）：

- 版本：`0.1.0-dev`
- EXE：`GodotVersion/build/windows/PCBuildingLife.exe`，119,000,560 bytes
- ZIP：`GodotVersion/build/windows/PCBuildingLife-Windows-x64-0.1.0-dev.zip`，约 47.7 MB
- SHA-256：`e559b17cf3c4e7e82241a31f571a946aca7bcdff88b7ebb1c54f037cb9ae41dd`
- 12 项 headless 测试通过，导出后启动冒烟通过。

验收：

- `GodotVersion/scripts/build_release.ps1` 单命令成功。
- `GodotVersion/build/windows/PCBuildingLife.exe` 可启动并停留在主菜单。
- ZIP 内包含 EXE 和 `release-manifest.json`。

### R1 玩家体验基线（已完成）

- [x] 主菜单增加继续游戏、新游戏、版本信息和退出游戏。
- [x] 主菜单移除背景图片和不可点击的商店/工作台 UI，改用纯色与程序化网格，并完成 1280x720 截图检查。
- [x] 设置支持主音量、全屏/窗口和三档分辨率，并持久化到 `user://settings.cfg`。
- [x] 增加制作信息和退出确认。
- [x] 游戏内增加暂停菜单、保存、保存并返回主菜单和设置入口。
- [x] 首次启动引导覆盖接单、购买安装、开机、Driver Tool、跑分和交付。
- [x] 增加坏存档识别、上一版有效存档备份、恢复与坏档归档提示。

R1 检查点构建（2026-07-11）：

- 主菜单、继续/新游戏、设置持久化行为测试通过。
- 主菜单与设置弹窗 1280x720 截图人工检查通过。
- MCP UI smoke 通过。
- 13 项 headless 测试通过，导出后启动冒烟通过。
- ZIP：`GodotVersion/build/windows/PCBuildingLife-Windows-x64-0.1.0-dev.zip`
- EXE：118,961,080 bytes；ZIP：47,645,541 bytes。
- SHA-256：`1c4b76352ce9a7778d4aaa1c880d717b2b09ecd8be30852f8d55cdeb5de55be9`

R1 暂停菜单检查点构建（2026-07-12）：

- 14 项 headless 测试通过，导出后启动冒烟通过。
- EXE：118,964,744 bytes；ZIP：47,649,287 bytes。
- SHA-256：`385b43ffc2964da495974482b492d75f7c121259cd5aa946f0e71d37b19f7091`

R1 纯功能 UI 检查点构建（2026-07-12）：

- 主菜单移除背景图，使用可伸缩纯色网格；制作信息与退出确认弹窗截图检查通过。
- 装机面板移除不可点击的工作台预览，只保留可点击、可拖放的机箱槽位视图。
- release 过滤未引用的商店、任务和工作台参考图，EXE/ZIP 包体明显缩小。
- MCP UI smoke 通过，软件配置分数为 100；14 项 headless 测试及导出后启动冒烟通过。
- EXE：112,335,648 bytes；ZIP：41,216,627 bytes。
- SHA-256：`651e71e889854694936e25f2a907db78547065724f9c424fc3822c14df666e97`

R1 玩家体验基线完成构建（2026-07-12）：

- 新手引导扩展为 9 步可操作流程，动作按钮可直接打开订单、选择缺失槽位、进入商店、执行检测、启动系统、打开 Driver Tool、完成驱动流程、跑分和交付。
- 自动保存采用临时文件校验后替换；覆盖有效存档前生成 `.bak`，损坏存档可恢复上一版或归档后开始新游戏。
- `GodotVersion-onboarding-check-1280x720.png` 与 `GodotVersion-save-recovery-check-1280x720.png` 人工检查通过，无重叠、截断或底层误点击入口。
- MCP UI smoke 识别到 114 个 UI 节点并确认引导可见；Driver Tool、软件配置 100 分和截图接口通过。
- 16 项 headless 测试通过，导出后启动冒烟通过。
- EXE：112,343,152 bytes；ZIP：41,224,101 bytes。
- SHA-256：`b0a70dcc8dd9b8beb0a9b5a5792d5e894babfe73d4c176c1c9185f5b27f8edec`

验收：玩家从首次启动到完成第一单不需要开发者解释或金手指。

### R2 内容与节奏（已完成）

- [x] 扩充订单数量、难度分层和客户类型。
- [x] 不同订单要求不同软件任务：基础驱动、显卡驱动、跑分、稳定性测试。
- [x] 调整配件价格、奖励、评分和失败成本，形成可持续经济循环。
- [x] 增加短期目标、阶段解锁与至少 60 分钟首发内容。
- [x] 存档版本升到 v2，保存并恢复可接订单池；旧存档缺字段时仍按已完成订单推导解锁。
- [x] 发布构建时排除 MCP addon，并临时移除 MCP autoload/editor plugin，导出后自动恢复开发配置。

R2 内容与节奏完成构建（2026-07-12）：

- 订单池扩展为 12 单；开局 3 单，后续按完成数量逐步解锁；客户类型 7 类，难度 1-5。
- 软件任务覆盖 `drivers`、`gpu_driver`、`benchmark`、`stability`；旗舰订单软件配置分数可达 100。
- `verify_content_progression.gd` 验证 12 单总估算 60 分钟内容，总花费 156,941，总奖励 221,500，最低资金 15,559，最终资金 84,559。
- `GodotVersion-r2-orders-check-1280x720.png` 与 `GodotVersion-r2-stability-check-1280x720.png` 人工检查通过；金手指移到左下角，不再遮挡订单详情；Max Monitor 可停留在 Stability Test 页面。
- MCP UI smoke 通过，识别到 117 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 18 项 headless 测试通过，Windows 导出后启动冒烟通过。
- EXE：112,226,176 bytes；ZIP：41,115,279 bytes。
- SHA-256：`38dc2640851f0a15dee5a83c196bf7cc318a651a9aabb97c78b6e1fd711438bc`

验收：连续游玩 30 分钟无内容断层，至少存在三种有差异的订单解法。

### R3 视觉、音频与反馈（进行中）

- [x] 统一主菜单、工作台、商店、背包、订单和模拟 OS 的视觉语言；主菜单已改为真实存档工作台，首页底部为全宽流程栏，旧商店/背包 Tab 已隐藏，工作台不再依赖不可点背景图。
- [x] 商店和背包改为卡片网格与右侧配件详情预览，保留购买并安装、安装、出售和拖拽安装路径。
- [x] 首页底部改为全宽 `HomeBottomDock`，三卡重排为 `工作流`、`订单`、`系统` 摘要区，压缩订单与系统摘要文案，减少 tab 来回切换。
- [x] 首页高频入口改为按钮/overlay 驱动：隐藏右侧 `TabContainer` 的旧标签，底部 Dock 的 `任务中心 -> 订单大厅 -> 交付订单 -> 系统中心 -> Max Monitor` 已接入键盘/手柄焦点顺序。
- [x] 增加购买、安装、开机、驱动安装、跑分、交付的程序化短音效底座。
- [x] 增加交付评分回执条，成功显示订单名、奖励、评分、跑分和评分拆分；失败显示交付前检查阻塞项。
- [x] 增加购买、安装、开机、驱动安装、跑分、交付的关键动画。
- [x] 完成奖励、错误、兼容性和软件配置状态的全局清晰反馈。
- [x] 每轮 UI 修改继续执行 1280x720 截图和 MCP 检查，并补充 1920x1080、1366x768。

R3 反馈闭环检查点构建（2026-07-13）：

- `verify_feedback.gd` 覆盖失败交付回执、成功交付回执、奖励、订单名和程序化音效节点。
- `GodotVersion-r3-feedback-check-1280x720.png`、`GodotVersion-r3-feedback-check-1366x768.png`、`GodotVersion-r3-feedback-check-1920x1080.png` 已生成并人工检查通过；回执条无重叠、溢出或遮挡。
- MCP UI smoke 通过，识别到 120 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 19 项 headless 测试通过，Windows 导出后启动冒烟通过。
- EXE：112,230,624 bytes；ZIP：41,118,725 bytes。
- SHA-256：`15ac528dfe9e5ba51de15c895d213e9cd80dbfbe63a5ab0efdd71afec9673b05`

R3 商店/背包卡片化检查点构建（2026-07-13）：

- `verify_catalog_layout.gd` 覆盖商店卡片数量、背包卡片数量、1280x720 首行 3 张卡片和右侧详情预览。
- `verify_catalog_screenshot.gd` 生成 `GodotVersion-r3-shop-cards-check-1280x720.png`、`GodotVersion-r3-inventory-cards-check-1280x720.png`、`GodotVersion-r3-shop-cards-check-1366x768.png`、`GodotVersion-r3-inventory-cards-check-1366x768.png`、`GodotVersion-r3-shop-cards-check-1920x1080.png`、`GodotVersion-r3-inventory-cards-check-1920x1080.png`；人工检查通过，卡片、详情图和操作按钮无重叠或遮挡。
- 关键回归串行通过：`verify_shop_filters.gd`、`verify_catalog_layout.gd`、`verify_workbench_overlay.gd`、`verify_onboarding_flow.gd`、`verify_guided_onboarding.gd`、`verify_software_delivery.gd`、`verify_feedback.gd`。
- MCP UI smoke 通过，识别到 120 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 20 项 headless 测试通过，Windows 导出后启动冒烟通过；release ZIP 仅包含 `PCBuildingLife.exe` 和 `release-manifest.json`，临时 release backup 无残留，开发项目 MCP 配置已恢复。
- EXE：112,232,176 bytes；ZIP：41,120,150 bytes。
- SHA-256：`cd652066971221f7f7cd7ed8165dcafb94a1226d3b7476b0aab37082ce8a9ad6`

R3 全局操作反馈检查点构建（2026-07-13）：

- 新增 `ActionFeedbackPanel`，以固定紧凑反馈条显示购买成功/失败、购买并安装、兼容性阻止、拖拽安装、出售回收、开机检测、Max Monitor、Driver Tool、Benchmark、稳定性测试和交付奖励结果。
- 订单回执移动到流程页顶部，修复新增反馈条后 1280x720 下回执被顶出视口的问题。
- `verify_action_feedback.gd` 覆盖资金不足、购买成功、兼容性阻止、驱动验证完成和交付奖励反馈。
- `verify_action_feedback_screenshot.gd` 生成 `GodotVersion-r3-action-feedback-check-1280x720.png`、`GodotVersion-r3-action-feedback-check-1366x768.png`、`GodotVersion-r3-action-feedback-check-1920x1080.png`；人工检查通过，反馈条不遮挡商店、背包、流程页和操作按钮。
- `verify_r3_screenshot.gd` 重新生成三档回执截图并通过；`verify_catalog_screenshot.gd` 重新生成商店/背包卡片截图并通过。
- MCP UI smoke 通过，识别到 122 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 21 项 headless 测试通过，Windows 导出后启动冒烟通过；release ZIP 仅包含 `PCBuildingLife.exe` 和 `release-manifest.json`，临时 release backup 无残留，开发项目 MCP 配置已恢复。
- EXE：112,236,688 bytes；ZIP：41,124,359 bytes。
- SHA-256：`01264349d14641fe788a46603f6947ba18c360f114d668c84307e70bc398e7a8`

R3 关键动画检查点构建（2026-07-13）：

- 购买成功/失败会 pulse 资金和状态；购买并安装、背包安装和拖拽安装会 pulse 对应槽位、机箱覆盖点和装机进度；开机检测和模拟系统启动会 pulse 工作台状态。
- Driver Tool、基础驱动安装、重启验证、GPU 驱动、Benchmark 和稳定性测试会 pulse OS/Monitor 文本区域；交付成功会 pulse 资金、状态和订单回执。
- `verify_key_animations.gd` 覆盖购买、安装、开机、驱动验证、Benchmark 和交付的动画事件计数与目标节点 pulse。
- `verify_key_animation_screenshot.gd` 生成 `GodotVersion-r3-key-animation-check-1280x720.png`、`GodotVersion-r3-key-animation-check-1366x768.png`、`GodotVersion-r3-key-animation-check-1920x1080.png`；人工检查通过，动画状态不遮挡商店、工作台、流程页和按钮。
- `verify_r3_screenshot.gd`、`verify_catalog_screenshot.gd`、`verify_action_feedback_screenshot.gd` 和 `verify_key_animation_screenshot.gd` 全部通过。
- MCP UI smoke 通过，识别到 122 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 22 项 headless 测试通过，Windows 导出后启动冒烟通过；release ZIP 仅包含 `PCBuildingLife.exe` 和 `release-manifest.json`，临时 release backup 无残留，开发项目 MCP 配置已恢复。
- EXE：112,239,872 bytes；ZIP：41,127,282 bytes。
- SHA-256：`bb8e1799fc02fc65dffa5bed51c22f7ac7fd52762bcbc91b5789b1fb0b2dff39`

R3 订单大厅重设计检查点（2026-07-13）：

- 引入 `game-ui-ux`、`godot-ui-control` 与 `frontend-design` 的组合工作流：游戏界面先按 screen/overlay、焦点、分辨率和安全边距设计，再落到 Godot `Control`/`Container`。
- 订单 tab 从窄列表改为底部摘要卡，只保留当前订单摘要、队列数量、`订单大厅` 和 `接取下一单` 快捷操作。
- 新增全屏 `订单大厅` overlay：左侧工单队列，中间显示客户、状态、难度、奖励、预计时间、硬件要求和软件配置，右侧为派工评估；支持当前订单返回工作台、可接订单接单、双击接单和 `ui_cancel`/Esc 关闭。
- [x] 订单大厅增补三栏任务板：派工评估栏展示装机范围、收益预估、风险提示、交付评分和解锁节奏，并提供 `打开任务中心`、`打开配件市场` 快捷操作。
- `verify_order_desk.gd` 覆盖订单大厅打开、列表数量、全屏布局、详情不重叠、接单后关闭和 Esc 关闭；已加入 `build_release.ps1` 测试队列。
- `verify_order_desk_screenshot.gd` 生成 `GodotVersion-order-desk-check-1280x720.png`、`GodotVersion-order-desk-check-1366x768.png`、`GodotVersion-order-desk-check-1920x1080.png`；人工检查通过，订单不再挤在窄 tab 中，按钮不贴边，背景不透底。
- `verify_r2_screenshot.gd` 已切换为订单大厅截图，并兼容 Windows 高 DPI：保存目标分辨率 PNG，同时记录物理渲染尺寸。
- MCP UI smoke 改用独立端口 `6516`，避免连接到用户已打开的编辑器实例；本轮通过 UI 枚举冒烟，识别到 190 个 UI 文本并确认引导可见、状态可读、截图接口可用。深层 Driver Tool/软件配置流程由 headless Godot 脚本继续覆盖。

验收：三个目标分辨率无重叠、溢出和不可点击控件。

R3 全屏目录覆盖层检查点（2026-07-13）：

- [x] 商店和背包新增全屏目录覆盖层，保留原右侧 tab 小面板作为兼容入口和快速状态视图。
- [x] 全屏目录支持“市场 / 仓库”模式切换、资金显示、Opaque 背景、大卡片网格、右侧详情预览、Esc/ui_cancel 关闭和焦点返回。
- [x] `verify_catalog_overlay.gd` 覆盖打开、全屏尺寸、首行 4 张以上卡片、详情预览、操作按钮启用和 Esc 关闭，并加入 `build_release.ps1`。
- [x] `verify_catalog_overlay_screenshot.gd` 生成 1280x720、1366x768、1920x1080 的市场/仓库截图，作为每轮 UI 视觉检查输入。

验收：玩家可以在全屏目录里完成购买、购买并安装、查看库存、安装和出售，不再依赖频繁切换窄右侧 tab。

R3 任务中心全屏化检查点（2026-07-13）：

- [x] 新增全屏 `任务中心` overlay，聚合原底部 `流程 / 订单 / 系统` 的核心信息：当前订单、硬件进度、软件配置、下一步动作、交付前检查和最近回执。
- [x] 任务中心顶部补充资金、进度、可接数量和系统状态芯片，继续把长文本摘要压成一眼可扫的驾驶舱。
- [x] 任务中心头部摘要进一步压缩为当前订单、软件、交付状态和下一步，减少与状态芯片的重复信息。
- [x] 顶部固定入口新增 `任务中心`；任务中心内提供 `订单大厅`、`配件市场`、`仓库`、`硬件检查`、`打开显示器`、`交付订单` 和动态下一步按钮。
- [x] 动态下一步不再只依赖新手引导：会按当前状态路由到补件、硬件检查、开机、Driver Tool、GPU 驱动、Benchmark、Stability Test 或交付。
- [x] `verify_task_center_overlay.gd` 覆盖打开、全屏尺寸、焦点、Esc 关闭、从任务中心打开市场，以及从任务中心完成检查/OS 软件配置/交付闭环，并加入 `build_release.ps1`。
- [x] `verify_task_center_screenshot.gd` 生成 1280x720、1366x768、1920x1080 的任务中心截图，作为底部流程/订单/系统重设计的视觉验收输入。

验收：玩家可以不依赖底部小 tab，在任务中心里看清当前订单、下一步、硬件/软件完成度和交付结果，并直接进入对应全屏界面或执行关键操作。

R3 首页工作流重排检查点（2026-07-15）：

- [x] 首页旧 `流程 / 订单 / 系统` 窄 tab 改为 `HomeBottomDock` 三块工作区：`工作流`、`订单`、`系统`，并保留任务中心、订单大厅、交付、系统信息、跑分、关机和 Max Monitor 直接入口。
- [x] 左侧工作台新增 `WorkbenchFooter` 快捷条，展示当前订单、完成度、缺失项和下一步，并提供 `订单大厅`、`完成检测`、`电源按钮` 近场操作。
- [x] `订单` 和 `系统` 工作区从纯文本状态升级为状态芯片 + 进度条：订单卡显示交付状态、预估评分和硬件完成度；系统卡显示开机状态、软件配置分数和软件完成度。
- [x] 金手指浮动按钮上移，避免遮挡工作台快捷条；`verify_ui_screenshot.gd` 增加 footer 可见、下一步文案和金手指不重叠断言。
- [x] 系统卡进一步收束为 `系统中心` + `Max Monitor` 两个入口：`系统中心` 打开全屏系统面板，聚合电源、系统信息、驱动、跑分、稳定性、文件和关机；`verify_system_center_overlay.gd`、`verify_system_center_screenshot.gd`、`verify_ui_screenshot.gd` 和 MCP UI smoke 均通过。
- [x] 修复 `verify_r3_screenshot.gd` 的窗口准备流程，三档截图保存目标尺寸 PNG，避免 1920x1080 档受当前窗口状态影响。

验收：`verify_ui_screenshot.gd`、`verify_workbench_screenshot.gd`、`verify_action_feedback_screenshot.gd`、`verify_r3_screenshot.gd`、`verify_key_animation_screenshot.gd`、工作台/订单/任务中心/目录/金手指/动作反馈回归和 MCP UI smoke 均通过；1280x720 截图人工检查无遮挡。

### R4 发布质量（进行中）

- [x] 增加跨版本存档迁移和向后兼容策略。
- [x] 验证低配性能、长时间运行、反复读档、连续订单与异常退出。
- [x] 对打包后的 EXE 执行完整玩家流程测试，而非只测编辑器项目。
- [x] 清理 release 构建中的开发工具、测试入口和未使用资源。

R4 存档迁移检查点构建（2026-07-13）：

- `load_game` 增加读取期迁移：无版本旧档和 v1 存档会在内存中升级到 `SAVE_VERSION = 2`，补齐 OS/驱动/Benchmark/稳定性/教程/订单池字段。
- `GameSession` 将无版本旧档识别为可继续存档，拒绝未来版本存档，避免新版档被旧客户端误读；迁移失败不会污染当前游戏状态。
- `verify_save_migration.gd` 覆盖无版本旧档、v1 缺少 `available_order_indices`、未来版本拒绝、迁移后再次保存写回 v2。
- MCP UI smoke 通过，识别到 122 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 23 项 headless 测试通过，Windows 导出后启动冒烟通过；release ZIP 仅包含 `PCBuildingLife.exe` 和 `release-manifest.json`，临时 release backup 无残留，开发项目 MCP 配置已恢复。
- EXE：112,243,024 bytes；ZIP：41,130,749 bytes。
- SHA-256：`3de92f5343e80fee3518c309947a34bfdbc52a34180f2154c7b9a3dafc7b89b5`

R4 稳定性压力检查点构建（2026-07-13）：

- 新增 `verify_release_stability.gd`，连续完成 12 个首发订单，并在每单后执行保存和 2 次读档回归。
- 稳定性脚本覆盖 14 次保存、25 次读档、异常退出残留 `.tmp` 不影响主档读取、坏主档通过 `.bak` 恢复，以及 480 帧 headless 帧推进预算。
- 本轮稳定性数据：12 单完成，最低资金 15,559，最终资金 84,559，默认渲染后端帧推进 3302ms / 8000ms，`gl_compatibility` 后端帧推进 3301ms / 8000ms。
- MCP UI smoke 通过，识别到 122 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 24 项 headless 测试通过，Windows 导出后启动冒烟通过；release ZIP 仅包含 `PCBuildingLife.exe` 和 `release-manifest.json`，临时 release backup 无残留，开发项目 MCP 配置已恢复。
- EXE：112,243,024 bytes；ZIP：41,130,751 bytes。
- SHA-256：`7f0de4292637b3daef538adbb45026b7aa0f06d99d27bc70a73ca702b0074633`

R4 导出 EXE 玩家流程检查点构建（2026-07-13）：

- 新增内部验证 preset `Windows Desktop Player Flow`，只有带 `pcbl_player_flow` feature 的验证包会响应 `--pcbl-release-flow` 并启动 `release_player_flow.gd`。
- `release_player_flow.gd` 在内部导出的 EXE 内完成 12 个首发订单，覆盖购买并安装、开机检测、电源按钮启动模拟 OS、Driver Tool、GPU 驱动、Benchmark、稳定性测试、订单交付、保存和读档；验证后删除内部 EXE。
- 公开 `Windows Desktop` preset 默认排除 MCP、`verify_*`、`mcp_ui_smoke.mjs`、`build_release.ps1` 和 `release_player_flow.gd`；`build_release.ps1` 会对最终公开 EXE 执行 public release guard，确认隐藏玩家流入口不会生成报告。
- `build_release.ps1` 在 Windows 导出和 4 秒启动冒烟后，启动导出的 `PCBuildingLife.exe --headless -- --pcbl-release-flow`，读取 `release-player-flow-report.json` 并将结果写入 manifest。
- 导出 EXE 玩家流程结果：12 单完成，13 次保存，12 次读档，最低资金 15,559，最终资金 84,559，帧推进 1644ms / 8000ms。
- MCP UI smoke 通过，识别到 122 个 UI 文本，确认引导可见、Driver Tool 可读、软件配置分数 100、截图接口可用。
- 24 项 headless 测试通过，Windows 导出、启动冒烟和导出 EXE 玩家流程通过；release ZIP 仅包含 `PCBuildingLife.exe` 和 `release-manifest.json`，临时 release backup 无残留，开发项目 MCP 配置已恢复。
- EXE：112,251,400 bytes；ZIP：41,138,711 bytes。
- SHA-256：`9915872aeb690d78edd5d35d4e859276785c7c9b48873b28bd7779a31ecba212`

验收：候选版连续完成全部首发订单，无崩溃、阻塞和存档丢失。

### R5 上线交付（进行中）

- [x] 确定候选版名称、版本号、版权和第三方素材许可边界；当前运行时配件图、PC 预览、应用图标、正式封面和渠道头图已替换为自有程序化素材。
- [x] 准备玩家 README、操作说明、候选版更新日志和上传前交付清单。
- [x] 生成候选 ZIP、校验值和发布说明，并增加 ZIP 解压后启动冒烟验收。
- [x] 增加导出后首单审计构建，在未依赖 Godot 编辑器的干净临时目录中完成第一单、保存并读档。
- [x] 增加独立模拟下载包验收脚本，校验 ZIP、manifest、反馈入口、干净目录解压和启动冒烟。
- [ ] 在公开下载页或另一台干净 Windows 机器上完成下载、解压、启动、第一单和保存验收。
- [x] 准备商店介绍、发布页候选截图、素材索引和隐私说明草案。
- [x] 准备正式封面、渠道头图和最终版权声明。
- [x] 补充真实上线渠道的反馈地址、邮箱或 Issue 表单。
- [x] 准备首发渠道页面草稿和本地上传 bundle。
- [x] 准备本地静态下载页，并校验页面资源、下载链接、校验值和反馈入口。
- [x] 增加本地静态下载页桌面/手机截图验收，避免首发页面出现标题重叠、按钮溢出或移动端不可点。
- [x] 增加公开下载验收脚本，可在真实 URL 上传后下载 ZIP 与 SHA 文件，复用包审计逻辑完成校验、解压和启动冒烟。
- [x] 增加 itch.io 草稿页 staging 脚本，生成上传目录、manifest、素材集合和 butler 命令文件。
- [x] 增加外部试玩测试包和一键启动器，方便私域或首批玩家按统一清单完成首单、保存和反馈。
- [ ] 选择首发渠道并完成页面、上传、审核与上线后崩溃反馈入口。

R5 候选包检查点（2026-07-19）：

- 发布脚本新增 `clean_extract_smoke` 和首单审计：构建后将公开 ZIP 解压到临时目录启动；另导出内部 `pcbl_first_order_audit` 构建，在干净临时目录完成第一单、保存并读档，随后删除内部审计 EXE。
- 本轮默认发布验证命令：`GodotVersion/scripts/build_release.ps1`；编辑器内全量测试、导出 EXE 12 单玩家流程、导出后首单审计、公开包双 guard、公开 EXE 启动冒烟和 ZIP 解压启动冒烟均通过。
- Manifest：`tests=passed`、`launch_smoke=passed`、`clean_extract_smoke=passed`、`public_release_guard=passed`、`public_first_order_guard=passed`、`player_flow=passed`、`first_order_audit=passed`。
- 玩家流证据：12 单完成，13 次保存，12 次读取，最终资金 84,559。
- 首单审计证据：`社区办公机` 完成，保存 1 次、读取 1 次，奖励 6,500，评分 99 / S，最终资金 22,059。
- 随包文件：`PCBuildingLife.exe`、`release-manifest.json`、`README.txt`、`RELEASE_NOTES.md`；未包含 MCP、验证脚本、内部玩家流脚本、内部首单审计脚本或构建脚本。
- EXE：109,439,296 bytes；EXE SHA-256：`15c5e46e5d4d83254723177ed54256ae5995bb5c1c9cc426b87a73dbe36ad755`。
- ZIP：`PCBuildingLife-Windows-x64-0.1.0-dev.zip`，38,342,077 bytes；ZIP SHA-256：`fed7c7f6cf2b7594e30e0cda765280a8e405921a34729f2e536d3507f4466bdc`。

R5 UI 视觉验收补强（2026-07-20）：

- 新增 `GodotVersion/scripts/verify_ui_visual_screenshots.ps1` 作为 UI 改动后的统一截图入口，默认覆盖主菜单、首页底部 Dock、工作台、订单大厅、市场/仓库、任务中心、系统中心、Max Monitor、动作反馈和关键动画。
- 视觉截图脚本使用真实窗口运行 Godot，不使用 `--headless`；原因是 headless 渲染下 viewport texture 为空，无法可靠生成 UI 截图，渲染后端可通过参数切换以便排查本机驱动差异。
- 如果 Godot 在截图脚本已输出 `*_screenshot=ok` 后退出阶段触发本机渲染崩溃，视觉入口默认记 warning 继续；需要把退出码也作为硬门槛时使用 `-StrictExitCode`。
- 默认发布命令 `GodotVersion/scripts/build_release.ps1` 继续保持 headless 自动化测试、导出玩家流、首单审计和公开包 guard；UI 改动合并前额外执行视觉截图入口。
- 首页底部 Dock 调整为更清晰的三块工作区视觉层级：卡片标题、当前步骤、订单和系统状态字号上调，Dock 总高控制在 190，避免挤压 1280x720 工作台底部槽位标签。
- `verify_action_feedback_screenshot.gd`、`verify_key_animation_screenshot.gd`、`verify_system_center_screenshot.gd` 和 `verify_task_center_screenshot.gd` 增加 `--pcbl-size=宽x高` 单尺寸入口；视觉 wrapper 对动作反馈、关键动画、系统中心逐尺寸重启，规避连续跨尺寸截图时的渲染退出崩溃。
- 本轮验证：`verify_workbench_screenshot.gd`、`verify_home_bottom_dock_screenshot.gd`、`verify_ui_screenshot.gd` 通过；完整 `verify_ui_visual_screenshots.ps1` 通过并输出 `warnings=0`；人工抽查 1280x720 工作台/底部 Dock 截图，底部槽位、金手指、订单和系统卡片均无明显遮挡。

R5 发布素材检查点（2026-07-19）：

- 新增 `doc/发布页文案.md`、`doc/隐私说明.md` 和 `doc/素材版权与许可边界.md`。
- 发布页候选截图整理到 `GodotVersion/release/media/`，包含主菜单、工作台、订单大厅、配件市场、仓库、任务中心、系统中心、Max Monitor 和交付评分回执，并生成 `contact-sheet.png` 做九宫格快速检查。
- 新增 `GodotVersion/assets/original` 自有程序化素材，覆盖配件图标、配件详情预览和 PC 预览；`assets/images/Logo.png` 已替换为自有应用图标。
- 新增 `cover-1920x1080.png`、`channel-header-1920x620.png`、`small-cover-630x500.png` 和 `branding-sheet.png`，作为试玩候选版封面、渠道头图和小封面素材。
- 新增 `doc/版权与第三方许可说明.md`；Windows 导出元数据、随包 README 和发布说明已写入 Copyright (c) 2026 AKG Studio。
- `building_panel.gd`、`shop_panel.gd`、`inventory_panel.gd` 运行时 preload 已切换到 `assets/original`；`export_presets.cfg` 已排除 `assets/pc_creator2/*`、`assets/pc_creator2/item_icons/*` 和 `release/media/*`。
- 修复 `verify_r3_screenshot.gd` 在 Windows 高 DPI 下用目标尺寸误判真实 viewport 的问题；布局断言改用实际 viewport，输出仍保存为目标尺寸。
- 修复 `verify_main_menu_screenshot.gd`、`verify_monitor_screenshot.gd` 和 `verify_ui_screenshot.gd` 的高 DPI 保存尺寸归一化，确保 1280x720 文件名对应真实 1280x720 PNG。
- 图形验证：`verify_main_menu_screenshot.gd`、`verify_monitor_screenshot.gd`、`verify_ui_screenshot.gd`、`verify_workbench_screenshot.gd`、`verify_catalog_overlay_screenshot.gd`、`verify_catalog_screenshot.gd`、`verify_r3_screenshot.gd` 均通过；截图尺寸已用 Windows 图片库读取确认。
- 公开 EXE 字符串扫描未发现 `pc_creator2`、`release/media` 或 `item-icons-sheet`，可确认旧竞品目录、外部发布截图目录和临时检查图未进入公开包。
- 公开上架剩余阻塞：渠道上传和外部干净机器验证仍未完成。

R5 最终候选包复核（2026-07-19）：

- README、发布说明、导出元数据和版权文档更新后，重新运行默认发布脚本并通过全量发布验证。
- Manifest：`tests=passed`、`launch_smoke=passed`、`clean_extract_smoke=passed`、`public_release_guard=passed`、`public_first_order_guard=passed`、`player_flow=passed`、`first_order_audit=passed`。
- 玩家流证据：12 单完成，13 次保存，12 次读取，最终资金 84,559。
- 首单审计证据：`社区办公机` 完成，保存 1 次、读取 1 次，评分 99 / S。
- ZIP 内仅包含：`PCBuildingLife.exe`、`release-manifest.json`、`README.txt`、`RELEASE_NOTES.md`。
- 公开 EXE 字符串扫描未发现 `pc_creator2`、`release/media` 或 `item-icons-sheet`，只保留自有运行时素材路径 `assets/original`。
- EXE：109,439,808 bytes；EXE SHA-256：`ce3dcfb8a9744f94a760e03d16907e3863c15a8cf5b464e5f20ab578d97fd1cc`。
- ZIP：`PCBuildingLife-Windows-x64-0.1.0-dev.zip`，38,342,815 bytes；ZIP SHA-256：`7feb499294322c4903131964ffc8bda5ebbeec8d7bacf1f68d8486c014844bb0`。

R5 反馈入口检查点（2026-07-19）：

- 新增 GitHub Issue 玩家反馈模板：`.github/ISSUE_TEMPLATE/pcbuildinglife-bug-report.yml`。
- 统一反馈入口：`https://github.com/bumagh/PCBuildingLife/issues`。
- 发布页文案、隐私说明、版权与第三方许可说明、素材边界文档、随包 README 和随包发布说明均已引用同一反馈入口。
- 重新运行默认发布脚本，编辑器内全量测试、导出 EXE 12 单玩家流程、导出后首单审计、公开包双 guard、公开 EXE 启动冒烟和 ZIP 解压启动冒烟均通过。
- ZIP 内 `README.txt` 和 `RELEASE_NOTES.md` 已直接读取确认包含 GitHub Issues 反馈入口。
- EXE：109,439,808 bytes；EXE SHA-256：`ce3dcfb8a9744f94a760e03d16907e3863c15a8cf5b464e5f20ab578d97fd1cc`。
- ZIP：`PCBuildingLife-Windows-x64-0.1.0-dev.zip`，38,342,815 bytes；ZIP SHA-256：`7feb499294322c4903131964ffc8bda5ebbeec8d7bacf1f68d8486c014844bb0`。

R5 模拟下载包验收检查点（2026-07-19）：

- 新增独立上传前/上传后复核脚本：`GodotVersion/scripts/verify_release_package.ps1`。
- 脚本会复制 ZIP 和 `.sha256` 到临时下载目录，校验 SHA-256，确认 ZIP 仅包含 `PCBuildingLife.exe`、`release-manifest.json`、`README.txt`、`RELEASE_NOTES.md`。
- 脚本会读取 ZIP 内 `release-manifest.json`，确认 `tests`、`launch_smoke`、`clean_extract_smoke`、`public_release_guard`、`public_first_order_guard`、`player_flow` 和 `first_order_audit` 均为通过状态。
- 脚本会直接读取 ZIP 内 `README.txt` 与 `RELEASE_NOTES.md`，确认包含 `https://github.com/bumagh/PCBuildingLife/issues`。
- 脚本会解压到全新临时目录，确认 EXE 大小和 SHA-256 与 manifest 一致，并隐藏启动 4 秒做玩家包冒烟。
- 本轮命令 `GodotVersion/scripts/verify_release_package.ps1` 通过，报告写入 `GodotVersion/build/windows/release-package-audit-report.json`。
- 报告摘要：ZIP SHA-256 `7feb499294322c4903131964ffc8bda5ebbeec8d7bacf1f68d8486c014844bb0`；EXE SHA-256 `ce3dcfb8a9744f94a760e03d16907e3863c15a8cf5b464e5f20ab578d97fd1cc`；12 单玩家流通过；首单审计 `社区办公机` 评分 99 / S；反馈入口已验证。
- 说明：这一步证明本机模拟下载包可解压、可启动、包内容干净；真实公开下载页或另一台干净 Windows 机器的完整首单验收仍保留为上线前最终外部验证项。

R5 首发渠道页面与上传 bundle 检查点（2026-07-19）：

- 新增 `doc/首发渠道页面草稿.md`，整理 itch.io 草稿页、私域试玩群或临时下载页可直接复用的标题、副标题、短简介、长简介、下载说明、系统要求、已知限制、隐私与反馈、截图顺序和上传前最终检查。
- 新增上传包整理脚本：`GodotVersion/scripts/prepare_release_upload_bundle.ps1`。
- 脚本先运行 `verify_release_package.ps1`，再将 ZIP、`.sha256`、`release-manifest.json`、`release-package-audit-report.json`、随包 README、发布说明、封面、头图、截图和 Markdown 文档复制到 `GodotVersion/build/upload/PCBuildingLife-0.1.0-dev/`。
- 本轮命令 `GodotVersion/scripts/prepare_release_upload_bundle.ps1` 通过，生成 `README_UPLOAD.md` 和 `upload-manifest.json`。
- 上传 bundle 证据：ZIP SHA-256 `7feb499294322c4903131964ffc8bda5ebbeec8d7bacf1f68d8486c014844bb0`；EXE SHA-256 `ce3dcfb8a9744f94a760e03d16907e3863c15a8cf5b464e5f20ab578d97fd1cc`；media 文件 15 个；doc 文件 9 个；12 单玩家流通过；首单审计 `社区办公机` 评分 99 / S；反馈入口已验证。
- 说明：bundle 是本地上传准备目录，不等同于真实渠道已上传；仍需选择实际渠道并从公开页面或另一台干净 Windows 机器做最终玩家验收。

R5 本地静态下载页检查点（2026-07-19）：

- 新增静态页面模板：`GodotVersion/release/channel_page/index.html`。
- `prepare_release_upload_bundle.ps1` 会将静态页复制为 `GodotVersion/build/upload/PCBuildingLife-0.1.0-dev/index.html`，页面直接引用 bundle 内的 ZIP、`.sha256`、封面和截图。
- 页面第一屏直接展示游戏名、下载按钮、反馈入口、ZIP 大小、12 单玩家流和首单审计；后续区块包含下载前确认、校验值、核心玩法、截图和隐私反馈。
- 新增页面校验脚本：`GodotVersion/scripts/verify_release_landing_page.mjs`。
- 本轮命令 `node GodotVersion/scripts/verify_release_landing_page.mjs` 通过，确认页面包含当前 ZIP/EXE SHA-256、反馈入口、下载链接，且 8 个本地引用文件均存在。
- 说明：这是本地可预览的静态下载页，可作为 itch/网盘/静态站页面原型；真实公开渠道上线后仍需从公开地址下载并完成外部验收。

R5 UI 迭代后发布复核（2026-07-19）：

- 在最新的任务中心与工作流 UI 收束后，补做 workbench 三档截图回归，随后重新运行默认 `build_release.ps1`，编辑器内全量测试、导出 EXE 玩家流、导出后首单审计、公开包双 guard、公开 EXE 启动冒烟和 ZIP 解压启动冒烟均通过。
- 本轮默认发布结果：EXE `PCBuildingLife.exe`，109,441,040 bytes，SHA-256 `1630ae7a35d7995b2246d74ea2116245c017a45a974a38b188740281138fd204`；ZIP `PCBuildingLife-Windows-x64-0.1.0-dev.zip`，38,343,990 bytes，SHA-256 `58aeba026b6a05d494296b5cd1c7b3a914f13147eac0e5872ee1a868d03803a1`。
- 当前发布链路继续保留 `tests=passed`、`launch_smoke=passed`、`clean_extract_smoke=passed`、`public_release_guard=passed`、`public_first_order_guard=passed`、`player_flow=passed`、`first_order_audit=passed`。
- 说明：这一步确认最近的 UI 迭代没有破坏导出包、公开包 guard 或玩家流验证。

R5 首页底部 Dock 复核（2026-07-19）：

- 隐藏右侧主界面的商店/背包 `TabContainer` 标签，减少玩家在购买后频繁切换旧 tab 的暗示，入口改由 `配件市场`、`仓库`、底部 Dock 和 overlay 承担。
- 底部 Dock 关键按钮统一命名并接入焦点顺序：`HomeTaskCenterButton`、`HomeOrderDeskButton`、`HomeDeliverOrderButton`、`HomeSystemCenterButton`、`HomeSystemMonitorButton`。
- 右侧旧背包/商店列表区已替换为 `CatalogWorkspacePanel`，只保留当前槽位、订单筛选、仓库匹配数量和 `打开配件市场` / `打开仓库` 两个全屏目录入口，避免主界面继续出现旧 Tab 内容。
- 新增 `GodotVersion/scripts/verify_home_bottom_dock_screenshot.gd`，生成 1280x720、1366x768、1920x1080 首页底部截图，并校验旧 Tab 标签隐藏、Dock 不裁切、不覆盖右侧面板、按钮尺寸和键盘/手柄左右焦点链。
- 本轮命令 `verify_home_bottom_dock_screenshot.gd`、`verify_ui_screenshot.gd`、`verify_workbench_screenshot.gd`、`verify_catalog_overlay_screenshot.gd`、`verify_order_desk_screenshot.gd`、`verify_system_center_screenshot.gd`、`verify_shop_filters.gd`、`verify_catalog_overlay.gd`、`verify_gameplay.gd` 和 `verify_order_software_tasks.gd` 通过。
- 发布素材 `GodotVersion/release/media/02-workbench.png` 已替换为最新 1920x1080 首页底部 Dock + 配件工作区截图；重新运行 `prepare_release_upload_bundle.ps1` 和 `verify_release_landing_page.mjs` 通过，upload bundle 内同名图片 SHA-256 与源文件一致：`47c59463595bf209702cd66cc2f74df2bed99e14d008530724db2c07e70f6455`。

R5 全屏目录 UX 复核（2026-07-20）：

- 全屏 `配件市场` 新增状态条：当前槽位、资金、订单目标、兼容数量、订单可用数量和当前显示数量；购买按钮会直接显示价格或资金不足，购买并安装按钮会直接显示不兼容/资金不足原因。
- 全屏 `仓库` 新增状态条：当前槽位、库存数量、当前显示数量、可安装数量和订单可用数量；安装按钮会直接显示可安装或不兼容状态。
- `InventoryPanel` 补入 `game` 上下文，仓库卡片和详情现在可显示兼容性与订单匹配状态；全屏市场/仓库都补齐键盘/手柄焦点链。
- `verify_catalog_overlay_screenshot.gd` 扩展为三档分辨率市场/仓库截图验收，校验状态条、按钮尺寸、按钮不裁切、预览图、网格密度和焦点链；人工检查 1280x720 市场/仓库截图通过。
- 本轮命令 `verify_catalog_overlay_screenshot.gd`、`verify_catalog_overlay.gd`、`verify_shop_filters.gd`、`verify_gameplay.gd`、`verify_ui_screenshot.gd` 和 `verify_home_bottom_dock_screenshot.gd` 通过。
- 发布素材 `04-catalog-shop.png`、`05-catalog-inventory.png` 和 `contact-sheet.png` 已更新；源目录、upload bundle 和 playtest kit 解包目录 SHA-256 一致：商店 `1bda1f956ac69dc0b1fae4841bfc3cf26023f5da6d65bb148d2b8af5c9e0733d`，仓库 `c5f536b6e09c990ba665b961b98b705ef9565c1df819359de4893a39e4eaa597`，contact sheet `d4714c6f749db68404a528fbb83ff8505573f00341d9549cf3ccb6ead07bc1ff`。
- 重新运行 `prepare_release_upload_bundle.ps1`、`verify_release_landing_page.mjs`、`prepare_playtest_kit.ps1` 和 `verify_playtest_kit.ps1` 通过；当轮 playtest kit 为 41,495,198 bytes，SHA-256 `d20dd2a1eafc012681eaab66ea45a67f9ccf4f80a5855eee55da23a94b63a270`。后续订单大厅任务板更新后已生成新版 kit，见 `R5 订单大厅任务板复核（2026-07-20）`。

R5 订单大厅任务板复核（2026-07-20）：

- `订单大厅` 详情区新增三张交付准备度卡：硬件准备、软件准备、交付状态。当前订单会显示硬件完成项、软件完成度、待处理阻塞和评分预估；候选订单会显示需求规模、软件任务和奖励/预计时间。
- 订单大厅新增 `OrderDeskDeliverButton`，当前订单可直接触发交付检查；未通过时沿用现有交付前检查反馈，不绕过硬件、开机、模拟 OS 和软件任务规则。
- 订单大厅补齐键盘/手柄焦点链：工单队列 -> 返回/接单 -> 交付订单 -> 任务中心 -> 配件市场。
- `verify_order_desk.gd` 增加准备度卡、交付按钮和快捷入口断言；`verify_order_desk_screenshot.gd` 增加准备度卡三档截图、按钮不裁切、焦点链和正文可视高度检查。
- 本轮命令 `verify_order_desk.gd`、`verify_order_desk_screenshot.gd`、`verify_orders.gd`、`verify_software_delivery.gd`、`verify_task_center_overlay.gd`、`verify_catalog_overlay.gd`、`verify_gameplay.gd` 和 `verify_ui_screenshot.gd` 通过；1280x720 订单大厅截图人工检查通过。
- 发布素材 `03-order-desk.png` 和 `contact-sheet.png` 已更新；源目录、upload bundle 和 playtest kit 解包目录 SHA-256 一致：订单大厅 `abfe8fdc7ba6e1ffd0577a77c9ba9c532f84c722a32845a48fbdcb0e25f4cd5f`，contact sheet `db72c4c65f20986b830e324250151b7b435d423a41993db480bb8c6c822fb0c3`。
- 重新运行 `prepare_release_upload_bundle.ps1`、`verify_release_landing_page.mjs`、`prepare_playtest_kit.ps1` 和 `verify_playtest_kit.ps1` 通过；当轮 playtest kit 为 41,544,457 bytes，SHA-256 `46695f754c604874d651bc2554b4aa15959c9f67d8c63018ab4ab3fa15c38150`。后续模拟 OS 任务板更新后已生成新版 kit，见 `R5 模拟 OS 任务板复核（2026-07-20）`。

R5 模拟 OS 任务板复核（2026-07-20）：

- Max Monitor 新增 `MonitorTaskBoard`，在每个模拟 OS 应用顶部持续显示当前订单、软件配置完成度、下一步 OS 操作和各软件任务进度。
- Desktop 文案接入下一步提示；Files 应用扩展为轻量报告目录，包含 `current_order.txt`、`latest_score.log`、`device_report.sys` 和 `preflight_report.txt`，能查看订单需求、驱动扫描结果、跑分/稳定性和交付前阻塞。
- Driver Tool、Benchmark、Stability Test 与 Files 继续复用现有状态机；任务板只读取并呈现当前状态，不绕过驱动扫描、安装、重启、GPU 驱动、跑分、稳定性和交付检查规则。
- `verify_monitor_os.gd` 增加 OS 任务板推进和 Files 报告断言；`verify_monitor_screenshot.gd` 升级为 1280x720、1366x768、1920x1080 三档截图验收，并校验任务板可见、与应用内容不重叠。
- 本轮命令 `verify_monitor_os.gd`、`verify_monitor_screenshot.gd`、`verify_software_delivery.gd`、`verify_order_software_tasks.gd`、`verify_system_center_screenshot.gd`、`verify_task_center_screenshot.gd`、`verify_ui_screenshot.gd` 和 `verify_gameplay.gd` 通过；1280x720 Monitor 截图人工检查通过。
- 发布素材 `08-max-monitor.png` 和 `contact-sheet.png` 已更新；源目录、upload bundle 和 playtest kit 解包目录 SHA-256 一致：Max Monitor `9bc54595cd9cc3b2828fee0746be2f9e29e78690fae57a71c1ccda8016c0d0b7`，contact sheet `3ca8c1c2bd0ef4af7a1a6a70af595079dde4e63d37ce33e3c5c20359133d451c`。
- 重新运行 `prepare_release_upload_bundle.ps1`、`verify_release_landing_page.mjs`、`prepare_playtest_kit.ps1` 和 `verify_playtest_kit.ps1` 通过；最新 playtest kit 为 41,615,814 bytes，SHA-256 `f144ec69a8325874396d5f3b5efabf4a11a0b2d4dedc81e534413a0675203290`。

R5 本地下载页视觉验收检查点（2026-07-19）：

- 新增桌面浏览器截图验收脚本：`GodotVersion/scripts/verify_release_landing_page_visual.mjs`。
- 脚本自动查找 Edge/Chrome/Chromium，打开上传 bundle 内的 `index.html`，生成 1280x720 桌面截图和 390x844 手机截图，并校验 PNG 尺寸与文件大小。
- 首次截图发现桌面端 H1 与封面背景文字重叠、手机端反馈按钮横向溢出；已调整 `GodotVersion/release/channel_page/index.html` 的 hero 遮罩、标题字号和移动端按钮栅格。
- 重新运行 `prepare_release_upload_bundle.ps1`、`verify_release_landing_page.mjs` 和 `verify_release_landing_page_visual.mjs` 后通过。
- 截图证据输出到 `GodotVersion/build/upload/PCBuildingLife-0.1.0-dev/visual-checks/`。

R5 公开下载验收脚本检查点（2026-07-19）：

- 新增 `GodotVersion/scripts/verify_public_download.ps1`。
- 脚本支持本地候选包模式和真实 URL 模式；真实上传后可传入 `-PackageUrl` 与 `-Sha256Url`，下载后再调用 `verify_release_package.ps1` 复用同一套包内容、manifest、反馈入口、解压和 EXE 启动验收。
- 本地模拟命令 `GodotVersion/scripts/verify_public_download.ps1 -ExpectedPackageSha256 58aeba026b6a05d494296b5cd1c7b3a914f13147eac0e5872ee1a868d03803a1` 通过。
- 报告写入 `GodotVersion/build/windows/public-download-audit-report.json`，内部包审计报告写入 `GodotVersion/build/windows/public-download-package-audit-report.json`。
- 本轮证据：ZIP SHA-256 `58aeba026b6a05d494296b5cd1c7b3a914f13147eac0e5872ee1a868d03803a1`；EXE SHA-256 `1630ae7a35d7995b2246d74ea2116245c017a45a974a38b188740281138fd204`；12 单玩家流通过；首单审计 `社区办公机` 评分 99 / S；启动冒烟 4 秒通过。
- 说明：这一步让真实公开 URL 上传后的下载验收可一键执行；实际公开地址尚未产生，因此外部干净机器完整首单验证仍是最终上线前阻塞项。

R5 itch.io 草稿页 staging 检查点（2026-07-19）：

- 新增 `doc/itch.io首发草稿页上传清单.md`，记录 itch.io 页面字段、图片、butler 命令、上传后验收和最终外部首单检查。
- 新增 `GodotVersion/scripts/prepare_itch_upload.ps1`，默认不上传，只运行包审计并生成 `GodotVersion/build/itch/PCBuildingLife-0.1.0-dev/`。
- staging 目录包含玩家 ZIP、SHA、包审计报告、上传 bundle manifest、全部 Markdown 文档、封面/头图/小封面/contact sheet/branding sheet、`itch-upload-manifest.json` 和 `butler-command.txt`。
- 脚本源已保持 ASCII-only，避免 Windows PowerShell 5 解析中文文件名乱码；文档通过复制 `doc/*.md` 进入 staging。
- 本轮命令 `GodotVersion/scripts/prepare_itch_upload.ps1 -Hidden` 通过；本机未检测到 butler，因此只生成命令文件，没有上传。
- butler 命令文件已生成，默认 channel 为 `windows-demo`，使用 `--userversion 0.1.0-dev`；真实上传仍需用户提供 itch 账号/项目 slug 并登录 butler。

R5 外部试玩测试包检查点（2026-07-19）：

- 新增 `doc/外部试玩验收清单.md`，定义外部测试者需要记录的环境、首单路径、30 分钟观察点、反馈入口和结论格式，并优先使用 `START_PLAYTEST_KIT.cmd` 启动。
- 新增 `doc/外部试玩反馈汇总模板.md`，用于回收多名测试者报告后按 P0/P1/P2 分级，并给出公开上传、小修后上传或暂缓上传的 go/no-go 判定。
- 新增 `GodotVersion/scripts/prepare_playtest_kit.ps1`，生成 `GodotVersion/build/playtest/PCBuildingLife-0.1.0-dev/` 和 `PCBuildingLife-0.1.0-dev-playtest-kit.zip`。
- 新增 `GodotVersion/scripts/verify_playtest_kit.ps1`，校验 kit ZIP 的 SHA-256、必需文件、manifest、12 单玩家流证据、首单审计证据、docs、media、反馈汇总模板和报告模板发布可用性字段，并解出 kit 内玩家 ZIP 复用 `verify_release_package.ps1` 做干净目录解压和 EXE 启动冒烟。
- 新增 `GodotVersion/scripts/summarize_playtest_reports.ps1`，读取回收目录中的 `PLAYTEST_REPORT_*.md`，输出 JSON 和 Markdown 汇总，统计通过、可玩有问题、阻塞、推荐/等待修复/不应发布和 P0/P1/P2，并给出 `go`、`fix_then_retest`、`no_go` 或证据不足判定。
- playtest kit 包含玩家 ZIP、SHA、release manifest、包审计报告、公开下载模拟报告、筛选后的玩家/试玩 Markdown 文档、发布截图、`README_PLAYTEST.txt`、`TEST_REPORT_TEMPLATE.md`、`START_PLAYTEST_KIT.ps1`、`START_PLAYTEST_KIT.cmd` 和 `playtest-kit-manifest.json`；内部排期文档通过 `fixed-validation-commands` 标记排除，避免 kit SHA 自引用漂移。
- 初版命令 `prepare_playtest_kit.ps1` 和 `verify_playtest_kit.ps1` 通过；kit ZIP 40,894,311 bytes，SHA-256 `683aa3daa5faf3b121feb84be2538c95731d070365aa8b95f38a6dae4f21481a`；反馈汇总模板通过 `PCBL_PLAYTEST_FEEDBACK_SUMMARY_V1` 内容标记验收；内层玩家包审计报告写入 `GodotVersion/build/playtest/playtest-kit-package-audit-report.json`；包内 `media/02-workbench.png` 已同步为最新首页底部 Dock + 配件工作区截图。后续目录 UX 截图更新后已生成新版 kit，见 `R5 全屏目录 UX 复核（2026-07-20）`。
- 反馈汇总脚本已用空目录、3 份全通过样例和含 1 份阻塞样例验证：空目录输出 `need_more_reports`，当前包全通过样例输出 `go`，阻塞样例输出 `no_go`。
- 说明：playtest kit 是私域/首批测试者分发包，不等同公开上架；它把外部首单验证的输入和反馈回收格式固定下来。

R5 验证临时目录修复（2026-07-19）：

- `verify_release_package.ps1` 新增 `-TempBase` 参数，默认使用 `GodotVersion/build/temp`，避免 Windows 系统盘临时目录空间不足导致包解压失败。
- `verify_public_download.ps1` 默认工作目录改为 `GodotVersion/build/temp`。
- `verify_playtest_kit.ps1` 默认使用 `GodotVersion/build/playtest/temp` 解出 kit，并将内层玩家包审计报告保存在 playtest build 目录。
- 本轮已用新的临时目录重新跑通 `verify_release_package.ps1`、`verify_public_download.ps1` 和 `verify_playtest_kit.ps1`。

R5 外部试玩启动器可见性修复（2026-07-20）：

- 修复 `prepare_playtest_kit.ps1` 生成的 `START_PLAYTEST_KIT.ps1`：外部试玩启动器不再用 `-WindowStyle Hidden` 启动游戏，双击 `START_PLAYTEST_KIT.cmd` 后应直接显示游戏窗口。
- `verify_playtest_kit.ps1` 新增断言：启动器必须包含可见启动命令，并且不得包含 `-WindowStyle Hidden`，避免后续重新生成 kit 时把玩家窗口藏起来。
- `doc/外部试玩验收清单.md` 已补充启动步骤说明，要求启动后能直接看到游戏窗口。
- 本轮命令 `prepare_playtest_kit.ps1` 和 `verify_playtest_kit.ps1` 通过；新版 playtest kit SHA-256 以同目录 `.sha256` 文件为准，避免文档入包后反复产生哈希漂移。

R5 发布就绪 go/no-go 审计（2026-07-20）：

- 新增 `GodotVersion/scripts/verify_release_readiness.ps1`，把 Windows ZIP、release manifest、包审计、上传 bundle、本地下载页、itch staging、公开下载验收、playtest kit 和 GitHub Issue 反馈入口合并成一个机器可读 go/no-go 报告。
- 首次运行发现 itch.io staging 仍指向旧 ZIP SHA-256；随后重新运行 `prepare_release_upload_bundle.ps1`、`verify_release_landing_page.mjs` 和 `prepare_itch_upload.ps1 -Hidden`，使上传 bundle 与 itch staging 都同步到当前 ZIP。
- 最终 `verify_release_readiness.ps1` 通过，报告写入 `GodotVersion/build/release-readiness/release-readiness-0.1.0-dev.json` 和 `.md`，结论为 `ready_for_external_validation`，失败项 0，警告项 2。
- 剩余警告是预期上线外部门槛：itch staging 尚未通过 butler 上传；公开下载验收仍是本地模拟，真实 URL 或干净外部 Windows 机器首单验证仍未完成。

R5 支持信息收集器检查点（2026-07-20）：

- 新增 `GodotVersion/scripts/collect_support_bundle.ps1`，用于外部试玩或上线后问题定位；脚本生成 `PCBuildingLife-support-*.zip`，包含系统摘要、release manifest 摘要、存档摘要、设置文件摘要和最近 Godot 日志。
- 支持包只输出 `save_summary.json`，不直接复制原始 `save_game.json`；README 会提醒玩家分享前可自行检查并移除隐私内容。
- `prepare_playtest_kit.ps1` 会把 `COLLECT_SUPPORT_BUNDLE.ps1` 和 `COLLECT_SUPPORT_BUNDLE.cmd` 放入外部试玩包；`verify_playtest_kit.ps1` 和 `verify_release_readiness.ps1` 已加入支持包收集器断言。
- 本轮 `collect_support_bundle.ps1 -OutputDir GodotVersion/build/support-test`、`prepare_playtest_kit.ps1`、`verify_playtest_kit.ps1` 通过；支持包 ZIP 内容已确认不包含原始存档。

R5 公开包支持收集器与下载页校验收紧（2026-07-20）：

- 公开 Windows ZIP 现在随包包含 `COLLECT_SUPPORT_BUNDLE.ps1` 和 `COLLECT_SUPPORT_BUNDLE.cmd`，玩家无需下载外部试玩 kit 也能生成隐私安全的问题定位包。
- `verify_release_package.ps1` 校验公开 ZIP 的 6 个随包文件，并检查支持收集器只生成 `save_summary.json`、`log_summary.json`、`system_summary.json` 等摘要，不直接复制原始 `save_game.json`。
- `prepare_release_upload_bundle.ps1` 会把当前 ZIP 链接、SHA 链接、ZIP 大小、ZIP SHA-256 和 EXE SHA-256 自动注入下载页；如果模板字段无法匹配，会直接失败，避免静默发布过期页面。
- `verify_release_landing_page.mjs` 新增页面可见 ZIP 大小校验，`verify_release_readiness.ps1` 会继续守住上传 bundle、公开下载模拟、playtest kit 和支持收集器。
- 本轮 `prepare_release_upload_bundle.ps1`、`verify_release_landing_page.mjs` 和 `verify_release_landing_page_visual.mjs` 通过；下载页显示 `38,367,359 bytes`，ZIP SHA-256 为 `ceae9fb10f88fe87776bc0a8f1c61d484610588c74f64daadb44f575c9f0fe8b`，EXE SHA-256 为 `af817418dbe1c515ae0b5eef7b1e2e57dbf58bb375bd7ae7a247ebb97136e755`。
- 随后重新运行 `verify_public_download.ps1 -ExpectedPackageSha256 ceae9fb10f88fe87776bc0a8f1c61d484610588c74f64daadb44f575c9f0fe8b`、`prepare_itch_upload.ps1 -Hidden`、`prepare_playtest_kit.ps1`、`verify_playtest_kit.ps1` 和 `verify_release_readiness.ps1` 通过；playtest kit SHA-256 以同目录 `.sha256` 文件为准，避免测试包文档自引用导致哈希漂移；readiness 结论为 `ready_for_external_validation`，失败项 0，警告项 2。

R5 外部首单报告门禁收紧（2026-07-20）：

- `prepare_playtest_kit.ps1` 生成的 `TEST_REPORT_TEMPLATE.md` 现在预填游戏版本和当前玩家 ZIP Package SHA-256，外部测试者回填报告时会保留构建证据。
- `summarize_playtest_reports.ps1` 默认读取当前 `PCBuildingLife-Windows-x64-0.1.0-dev.zip.sha256`，只把 Package SHA-256 匹配当前 ZIP 的报告计入 `current_package_reports`、通过数和 go 判定；旧包或缺失哈希的报告会计入 `stale_or_unknown_package_reports`。
- `verify_release_readiness.ps1` 新增 `external_playtest_reports` 检查：`go` 时通过，`need_more_reports` 或证据不足时警告，`fix_then_retest` 或 `no_go` 时失败。
- 本轮 `summarize_playtest_reports.ps1` 在空回收目录下输出 `need_more_reports`；`prepare_playtest_kit.ps1`、`verify_playtest_kit.ps1` 和 `verify_release_readiness.ps1` 通过，readiness 结论仍为 `ready_for_external_validation`，失败项 0，警告项 3。新增警告是预期外部门槛：尚未回收 3 份当前包外部首单报告。

R5 外部验证轮次分发包（2026-07-20）：

- 新增 `GodotVersion/scripts/prepare_external_validation_round.ps1`，整理 `playtest-kit.zip`、kit SHA、测试者消息、回收 tracker、反馈汇总和 readiness 报告到 `GodotVersion/build/external-validation/PCBuildingLife-0.1.0-dev/`，并生成可直接分发的 `PCBuildingLife-0.1.0-dev-external-validation-round.zip` 与 `.sha256`。
- 该脚本会先运行 `verify_playtest_kit.ps1` 和 `summarize_playtest_reports.ps1`，生成 `external-validation-manifest.json` 后再运行 `verify_release_readiness.ps1` 并回填结果，记录当前玩家 ZIP SHA、playtest kit SHA、目标测试人数和现有报告数。
- `verify_release_readiness.ps1` 新增 `external_validation_round` 检查，确保外部验证轮次目录和 round ZIP 存在、ZIP SHA 校验通过、Package SHA 和 kit SHA 都匹配当前候选包、测试者数量不少于 3，且测试者消息和 tracker 文件齐全。
- 新增 `GodotVersion/scripts/verify_external_validation_round.ps1`，会解压 round ZIP，检查内层 playtest kit、SHA、测试者消息、tracker、反馈汇总证据和 manifest，并输出 `GodotVersion/build/external-validation/external-validation-round-audit-0.1.0-dev.json`。
- readiness 的 `external_validation_round` 会读取深度审计报告；如果 round ZIP 只存在但内容不完整、内层 kit 过期、tracker 缺当前 Package SHA 或审计报告过期，会直接失败。
- round ZIP 是发给测试者的稳定分发包；外部验证目录里的 `evidence/` 保留最新 readiness 与反馈汇总证据，避免把 readiness 报告塞回 ZIP 形成自引用哈希漂移。
- 这一步把“把包发给 3 个外部测试者”固定成可复用交付目录；下一轮回收报告后只需放入 `GodotVersion/build/playtest/returned-reports/`，再运行 `summarize_playtest_reports.ps1` 和 `verify_release_readiness.ps1`。

R5 itch.io staging 深度审计（2026-07-20）：

- 新增 `GodotVersion/scripts/verify_itch_staging.ps1`，检查 itch.io staging 目录中的玩家 ZIP、SHA、包审计报告、上传 manifest、文档、发布媒体图和 butler 命令文件。
- `verify_itch_staging.ps1` 会确认 staging ZIP 与 `.sha256`、`itch-upload-manifest.json` 和 `upload-manifest.json` 使用同一个当前 Package SHA-256，并确认 butler 命令包含 `windows-demo`、`--userversion 0.1.0-dev` 和真实公开下载验收命令里的当前 SHA-256。
- `verify_release_readiness.ps1` 的 `itch_staging` 门禁改为读取 `GodotVersion/build/itch/itch-staging-audit-0.1.0-dev.json`；审计缺失、失败或过期会直接失败，审计通过但还未 butler 上传则继续保留预期警告。
- 这一步把“只准备了 itch 草稿目录”升级为“草稿目录可以被机器独立验收”，避免后续文档或包重打后使用过期 staging。

R5 itch.io 上传配置固化（2026-07-20）：

- 新增公开模板 `GodotVersion/release/itch-upload-config.example.json`，记录 `itch_target`、`channel`、`hidden`、`if_changed`、真实公开 ZIP URL 和 `.sha256` URL 字段。
- 新增 `GodotVersion/scripts/verify_itch_upload_config.ps1`，校验配置 JSON、`itch-user/itch-game` 目标格式、channel 格式、隐藏/增量上传开关和公开下载 URL 成对填写规则；`-RequireTarget` 会拒绝占位目标。
- `GodotVersion/release/itch-upload-config.local.json` 已加入 `.gitignore`，真实 itch 项目 slug 只放本机配置，不写进仓库和发布资料。
- `prepare_itch_upload.ps1` 现在会优先读取本地配置，命令行参数仍可覆盖；支持 `-Public` 覆盖隐藏上传，`if_changed` 会写入 butler 命令和 manifest。
- `verify_release_readiness.ps1` 新增 `itch_upload_config` 检查：配置审计缺失会警告，配置失败会阻断，仍使用占位目标会提示真实上传前必须配置。

R5 干净机器首单验收包（2026-07-20）：

- 新增 `doc/干净机器首单验收清单.md`，固定另一台 Windows 或干净用户配置下的上线前首单验收步骤。
- 新增 `GodotVersion/scripts/prepare_clean_machine_validation_kit.ps1`，生成 `GodotVersion/build/clean-machine-validation/PCBuildingLife-0.1.0-dev/` 和可分发 ZIP。
- 验收包包含当前玩家 ZIP、SHA、release/package/public download 证据、关键文档、关键截图、`RUN_CLEAN_MACHINE_VALIDATION.cmd/.ps1`、`CLEAN_MACHINE_REPORT_TEMPLATE.md` 和 manifest。
- `RUN_CLEAN_MACHINE_VALIDATION.ps1` 可使用随包 ZIP，也可在本地配置存在公开 URL 后下载真实公开 ZIP；脚本会校验 SHA、解压、可见启动游戏，关闭后生成 `CLEAN_MACHINE_REPORT_*.md` 和机器环境 session JSON。
- 新增 `GodotVersion/scripts/verify_clean_machine_validation_kit.ps1`，校验 kit ZIP、自带玩家包 SHA、runner/report template 内容，并解出内层玩家 ZIP 复用 `verify_release_package.ps1` 做干净目录启动冒烟。
- `verify_release_readiness.ps1` 新增 `clean_machine_validation_kit` 检查，确保该验收包与当前候选包同哈希且深度审计通过；真实干净机器报告仍是外部门槛，不由本机 kit 审计替代。

R5 干净机器报告回收导入（2026-07-20）：

- 新增 `GodotVersion/scripts/receive_clean_machine_reports.ps1`，默认从 `GodotVersion/build/clean-machine-validation/incoming-reports/` 接收外部机器交回的 `CLEAN_MACHINE_REPORT_*.md` 和 `clean-machine-session-*.json`。
- 合格报告会复制到 `GodotVersion/build/clean-machine-validation/returned-reports/`，session JSON 会复制到 `GodotVersion/build/clean-machine-validation/returned-sessions/`；原 incoming 文件保留，便于追溯。
- 导入脚本会拒绝缺少测试者、日期、机器类型、游戏版本、Package SHA-256、包来源、Windows 版本、CPU/GPU、内存或分辨率字段的报告；Package SHA-256 不匹配当前 ZIP、结果/发布建议复选框不唯一、通过/可玩报告缺首单评分，或未勾齐“校验、解压启动、新游戏、首单、保存、重开、继续、无旧存档”路径，也会被拒绝。
- 新增 `GodotVersion/scripts/verify_clean_machine_report_intake.ps1`，自动生成 1 份当前包通过报告、1 份重复报告、1 份旧包报告、1 份未完成路径报告以及正常/损坏 session JSON，验证导入器能得到 `go` 样例汇总，同时拦截重复、旧包、路径未完成和坏 session。
- `verify_release_readiness.ps1` 新增 `clean_machine_reports` 检查；导入审计缺失或证据不足时警告，审计失败/过期、`fix_then_retest` 或 `no_go` 时失败，`go` 时通过。
- 本轮 `receive_clean_machine_reports.ps1` 在正式空回收目录下输出 `need_more_reports`，`verify_clean_machine_report_intake.ps1` 自测通过；重新生成并验证干净机器 kit、playtest kit 和外部验证 round，各分发 ZIP 的当前 SHA-256 以同目录 `.sha256` 和对应审计报告为准，避免文档入包后产生自引用哈希漂移。
- 本轮 `verify_release_readiness.ps1` 通过，readiness 结论仍为 `ready_for_external_validation`，失败项 0，警告项 5；新增预期警告是尚未回收真实干净机器首单报告。

R5 外部试玩报告回收导入（2026-07-20）：

- 新增 `GodotVersion/scripts/receive_playtest_reports.ps1`，默认从 `GodotVersion/build/playtest/incoming-reports/` 接收测试者交回的 `PLAYTEST_REPORT_*.md` 和 `PCBuildingLife-support-*.zip`。
- 合格报告会复制到 `GodotVersion/build/playtest/returned-reports/`，支持包会复制到 `GodotVersion/build/playtest/returned-support-bundles/`；原 incoming 文件不删除，便于追溯。
- 导入脚本会拒绝缺少测试者、测试日期、游戏版本、Package SHA-256、Windows 版本或分辨率字段的报告；Package SHA-256 不匹配当前 ZIP、结果复选框不唯一、发布建议复选框不唯一、通过/可玩报告缺首单结果，也会被拒绝。
- 支持包会被解压检查文件名，若包含原始 `save_game.json`、`settings.cfg`、`auth.json`、SQLite/DB 文件或超过 50 MB，会被拒绝，避免把隐私原件混入回收区。
- 导入完成后自动运行 `summarize_playtest_reports.ps1` 刷新 go/no-go 汇总，并输出 `GodotVersion/build/playtest/report-intake/playtest-report-intake-0.1.0-dev.json` 与 `.md`。
- `verify_release_readiness.ps1` 新增 `external_report_intake` 检查；导入审计缺失时警告，审计失败或过期时失败，审计通过但报告不足仍由 `external_playtest_reports` 保持警告。
- 新增 `GodotVersion/scripts/verify_playtest_report_intake.ps1`，自动生成 3 份当前包通过报告、1 份重复报告、1 份旧包报告以及安全/不安全支持包，验证导入器能得到 `go` 样例汇总，同时拦截重复、旧包和含原始存档的支持包。

R5 发布环境预检（2026-07-20）：

- 新增 `GodotVersion/scripts/verify_publish_environment.ps1`，把当前 Windows ZIP、itch.io 上传配置、butler 可执行状态、itch staging、butler 命令文件和公开下载 URL 验收合并成一份发布环境审计。
- 默认模式允许真实渠道仍未配置，只把 `itch_target` 占位、butler 未安装、公开 URL 未配置和未真实上传标记为预期 warning；加 `-RequireTarget`、`-RequireButler` 或 `-RequirePublicUrls` 时可作为真实上传前硬门禁。
- 审计报告输出到 `GodotVersion/build/publish/publish-environment-audit-0.1.0-dev.json/.md`，当前没有失败项，结论会停在 `waiting_for_channel_setup`，直到本机写入真实 itch 目标并安装/login butler。

R5 最终公开发布硬门禁（2026-07-20）：

- 新增 `GodotVersion/scripts/verify_public_release_go.ps1`，把 `release_readiness` 中仍允许作为 warning 的外部门槛升级为最终公开发布硬条件。
- 最终门禁要求：当前 ZIP SHA 匹配、release readiness 无失败、真实 itch 目标已配置、当前包已通过 `prepare_itch_upload.ps1 -Push` 上传、发布环境预检达到 `public_download_validated`、真实公开 URL 下载验收通过、至少 1 份当前包干净机器首单报告 `go`、至少 3 份当前包外部首单报告 `go`、外部验证 round 审计当前、反馈入口存在。
- 日常验证可用 `-AllowNoGo` 生成报告而不让命令失败；真正公开发布前必须不带 `-AllowNoGo` 运行并得到 `go_public_release`。
- 本轮 `verify_public_release_go.ps1 -AllowNoGo` 生成 `GodotVersion/build/public-release-go/public-release-go-0.1.0-dev.json/.md`；不带 `-AllowNoGo` 的命令按预期阻断当前候选包，结论为 `no_go_external_gates_required`，失败项 6。

R5 发布方交接包（2026-07-20）：

- 新增 `GodotVersion/scripts/prepare_publisher_handoff.ps1`，把当前 Windows 玩家包、上传 bundle、itch staging、butler 命令、干净机器 kit、外部试玩 kit、外部验证 round、release readiness、publish environment、public release gate 和关键文档整理到 `GodotVersion/build/publisher-handoff/PCBuildingLife-0.1.0-dev/`。
- 交接包同时生成 `PCBuildingLife-0.1.0-dev-publisher-handoff.zip` 和 `.sha256`，用于把“上传、找测试者、回收证据、最终 go/no-go”交给发布执行者；包内 `PUBLISHER_HANDOFF.md` 明确要求最终 `verify_public_release_go.ps1` 不带 `-AllowNoGo` 通过后才能扩大公开分发。
- 新增 `GodotVersion/scripts/verify_publisher_handoff.ps1`，会解压交接包，校验玩家 ZIP、三个验证 ZIP、上传 bundle、itch staging、证据报告、runbook 和 manifest 的哈希/版本一致性。
- 分发 ZIP 的当前 SHA-256 以同目录 `.sha256` 和 `publisher-handoff-audit-0.1.0-dev.json` 为准，避免把交接包自身哈希写入包内文档造成自引用漂移。

R5 配件工作区决策面板与发布回归（2026-07-26）：

- `GodotVersion/scripts/game.gd` 的首页“配件工作区”新增当前状态、订单压力和推荐操作，复用任务中心的下一步决策，并保留市场/仓库全屏入口，减少在商店、背包和工作台之间反复切换。
- `GodotVersion/scripts/verify_home_bottom_dock_screenshot.gd` 与 `GodotVersion/scripts/verify_ui_screenshot.gd` 新增工作区状态文案、订单压力、推荐操作和按钮焦点断言；1280x720、1366x768、1920x1080 真实窗口检查通过，完整视觉套件 `warnings=0`。
- `node GodotVersion/scripts/mcp_ui_smoke.mjs` 通过，Godot MCP 注册 173 个工具，真实 UI smoke 看到“交付订单”状态并完成截图。
- 核心 headless 回归通过：数据、装机、存档迁移、12 单稳定性、订单、兼容性、商店筛选、目录、引导、OS 软件任务、扩展流程、交付评分和反馈。
- `GodotVersion/scripts/build_release.ps1` 通过，玩家流和干净目录首单审计通过；当前 Windows ZIP SHA-256 为 `ec6b25d498d3da473875a8069114bf55e2b0c4b3ca5d1972a18fb0eac5065653`，EXE SHA-256 为 `84955fada7f569709555cc98988c0dc3f93117cb898ada8377ee2554e5f9066c`。
- 随后运行 `prepare_itch_upload.ps1 -Hidden`、`verify_itch_staging.ps1`、`prepare_playtest_kit.ps1`、`verify_playtest_kit.ps1`、`prepare_clean_machine_validation_kit.ps1`、`verify_clean_machine_validation_kit.ps1`、`prepare_external_validation_round.ps1` 和 `verify_external_validation_round.ps1`，当前候选包的本地分发资料均已重新对齐并通过审计。
- 本地 `verify_public_download.ps1 -ExpectedPackageSha256 ec6b25d498d3da473875a8069114bf55e2b0c4b3ca5d1972a18fb0eac5065653` 通过；最新 `verify_publisher_handoff.ps1` 通过，交接包 SHA-256 为 `8c643193ace7e1e43e5933bb8bf24a69fe59f46dd16cb72472ce7544f558fb34`。
- 当前 `verify_release_readiness.ps1` 为 `no_go_fix_required`，失败 1 项、警告 4 项；`verify_public_release_go.ps1 -AllowNoGo` 为 `no_go_external_gates_required`，尚未配置真实 itch 目标、butler 推送、真实公开 URL、1 份干净机器报告和 3 份外部试玩报告。
- 本轮同步更新 `doc/上线交付清单.md`、`doc/itch.io首发草稿页上传清单.md`、`doc/外部试玩反馈汇总模板.md` 和 `doc/干净机器首单验收清单.md` 的当前候选包 hash；历史排期记录保留旧 hash 作为历史证据。重新生成的 playtest kit SHA-256 为 `cc49de77dc5fe07420282335973a94733ef52d4bc6485809c6e5e1f9e9836307`，clean-machine kit SHA-256 为 `a7a6c9caa970a710a23674625637d4a01fd1af25f4f721a45702352c3cf3864d`，external round SHA-256 为 `c3eda3eac0350a004c324b106b41589831652154407aec6a0597839ec3fd10e3`。
- 当前唯一本地 readiness 失败项是尚未收到当前包的干净机器首单报告；winget 检查未在本机超时窗口内返回可用 butler 安装项，真实 itch 配置和 butler 登录仍需外部渠道状态。
- 下一步：继续完成真实 itch.io 目标配置、公开 URL 下载验收、干净机器报告和外部试玩报告，最终运行不带 `-AllowNoGo` 的公开发布门禁；本轮本机证据不替代这些外部门槛。

R5 发布资料重新对齐检查点（2026-07-26）：

- 按当前源码重新运行 `GodotVersion/scripts/build_release.ps1`，编辑器内全量回归、导出玩家流 12 单、首单审计、公开包 guard、公开 EXE 启动和 ZIP 解压启动均通过。
- 当前候选包：EXE `84955fada7f569709555cc98988c0dc3f93117cb898ada8377ee2554e5f9066c`；ZIP `94eabddb3855db3ecda92565be3ed516fa016e58b9f54c9acff590e165e1dcde`。
- 重新生成并验证 `prepare_release_upload_bundle.ps1`、`prepare_itch_upload.ps1 -Hidden`、`verify_itch_staging.ps1`、`prepare_playtest_kit.ps1`、`verify_playtest_kit.ps1`、`prepare_clean_machine_validation_kit.ps1`、`verify_clean_machine_validation_kit.ps1`、`prepare_external_validation_round.ps1`、`verify_external_validation_round.ps1`、`verify_public_download.ps1`、`prepare_publisher_handoff.ps1` 和 `verify_publisher_handoff.ps1`；本地分发资料均与当前 ZIP hash 一致。
- `verify_release_readiness.ps1` 当前输出 `ready_for_external_validation`，失败 0、警告 5；`verify_publish_environment.ps1` 输出 `waiting_for_channel_setup`，失败 0、警告 5。
- 仍未完成且不能由本机模拟替代的门槛：真实 itch 项目配置和 butler 上传、真实公开 URL 下载、至少 1 份干净 Windows 首单报告、至少 3 份当前包外部试玩报告；最终需不带 `-AllowNoGo` 运行 `verify_public_release_go.ps1` 并得到 `go_public_release`。
- 本轮未修改 UnityVersion，也未使用未授权竞品运行时素材；本机生成的分发 ZIP、报告和交接包均保留在 `GodotVersion/build/`。

R5 当前候选包与视觉门禁复核（2026-07-27）：

- 按最新源码重新运行 `GodotVersion/scripts/build_release.ps1`，28 项核心 headless 测试、导出 12 单玩家流、首单审计、公开包 guard、启动冒烟和干净目录解压启动均通过。
- 当前玩家 ZIP SHA-256：`c48a09d1e9df84170f00d24c39a58c37b4ec59a58e277bfb1cb2bf3862f0b358`；EXE SHA-256：`84955fada7f569709555cc98988c0dc3f93117cb898ada8377ee2554e5f9066c`。
- `verify_ui_visual_screenshots.ps1` 通过并输出 `warnings=0`，覆盖主菜单、首页底部 Dock、工作台、订单大厅、市场/仓库、任务中心、系统中心、Max Monitor、动作反馈和关键动画的三档分辨率截图。
- 当前 NVIDIA Vulkan 驱动会在部分截图脚本退出清理阶段产生原生退出码；视觉套件对 Max Monitor、系统中心和关键动画使用 `gl_compatibility` + `opengl3` 真实窗口后端，只有成功标记和截图均完成时才接受 post-capture 退出码，断言失败仍为硬失败。
- `node GodotVersion/scripts/mcp_ui_smoke.mjs` 通过，注册 173 个 MCP 工具，真实 UI 识别 251 个文本，确认新手引导可见、流程推进到“交付订单”并成功截图。
- 最新 `verify_public_download.ps1` 本地模拟公开下载通过；`verify_release_readiness.ps1` 输出 `ready_for_external_validation`，失败 0、预期外部警告 5。
- 当前公开发布门禁仍为 `no_go_external_gates_required`：真实 itch 目标与 butler 上传、真实公开 URL 下载、1 份干净 Windows 首单报告和 3 份当前包外部试玩报告仍缺失。不得用本机模拟下载或样例报告替代这些证据。

R5 当前候选包重建与交付资料再对齐（2026-07-27）：

- 按当前源码重新运行 `GodotVersion/scripts/build_release.ps1`，数据、装机、存档迁移、12 单稳定性、订单、兼容性、目录、引导、模拟 OS、软件任务、交付反馈等全量 headless 回归通过；导出 12 单玩家流、首单审计、公开包 guard、EXE 启动和 ZIP 解压启动均通过。
- 当前玩家 ZIP：`PCBuildingLife-Windows-x64-0.1.0-dev.zip`，38,370,447 bytes，SHA-256 `c48a09d1e9df84170f00d24c39a58c37b4ec59a58e277bfb1cb2bf3862f0b358`；EXE 109,464,688 bytes，SHA-256 `84955fada7f569709555cc98988c0dc3f93117cb898ada8377ee2554e5f9066c`。
- 重新生成并审计 `prepare_release_upload_bundle.ps1`、`prepare_itch_upload.ps1 -Hidden`、`verify_itch_staging.ps1`、`verify_public_download.ps1`、试玩 kit、干净机器 kit、外部验证 round 和发布方交接包；各分发 ZIP 的权威 SHA-256 以同目录 `.sha256` 文件和对应审计报告为准，避免把交付包自身哈希写入包内文档形成自引用漂移。
- 当前 `verify_release_readiness.ps1` 输出 `ready_for_external_validation`（0 失败、5 个预期外部警告）；`verify_publish_environment.ps1` 输出 `waiting_for_channel_setup`（0 失败、5 个警告）；`verify_public_release_go.ps1 -AllowNoGo` 输出 `no_go_external_gates_required`（6 个外部失败项）。
- 本轮更新 `doc/上线交付清单.md`、`doc/itch.io首发草稿页上传清单.md` 和 `doc/首发渠道页面草稿.md` 的当前 ZIP/EXE 哈希，避免源码重建后发布文档继续引用旧包；未修改 `UnityVersion`，未引入未授权竞品运行时素材。
- 下一步仍必须把 clean-machine kit 发到一台真实干净 Windows 机器、把 external-validation round 发给至少 3 名测试者；收到报告后分别运行 `receive_clean_machine_reports.ps1`、`receive_playtest_reports.ps1`、`verify_release_readiness.ps1`，并在真实 itch 目标/butler/公开 URL 可用后运行不带 `-AllowNoGo` 的最终门禁。

R5 发布环境工具校验修复（2026-07-27）：

- 从 itch.io butler 官方 GitHub `v15.29.0` Windows amd64 发行包安装本地 butler，版本命令实测通过；安装文件只放在用户本地 itch 应用目录和项目 `GodotVersion/tmp`，不进入玩家 ZIP。
- 修复 `GodotVersion/scripts/verify_publish_environment.ps1` 的 butler 版本探测：使用 `System.Diagnostics.Process` 捕获 stdout/stderr，避免 butler 将版本信息写入 stderr 时被 PowerShell 当作异常吞掉。
- `verify_publish_environment.ps1` 修复后输出 `waiting_for_channel_setup`，失败 0、警告 4，`butler_tool` 已为 pass；随后 `verify_release_readiness.ps1` 仍为 `ready_for_external_validation`（0 失败、5 警告），公开门禁仍按预期为 `no_go_external_gates_required`（6 个真实外部门槛）。
- 重新生成发布方交接包并通过 `verify_publisher_handoff.ps1`；真实 itch target、butler 登录/推送、公开 URL、干净机首单和 3 份当前包外部报告仍不能由本机模拟替代。

验收：玩家可从公开下载页获得构建、启动游戏、完成第一单并正常保存进度。

R5 发布方交接审计复核（2026-07-27）：

- 重新运行 `verify_publisher_handoff.ps1`，当前 `GodotVersion/build/publisher-handoff/PCBuildingLife-0.1.0-dev-publisher-handoff.zip` 通过审计；交接包内的玩家 ZIP、上传 bundle、itch staging、干净机 kit、外部试玩 kit、外部验证轮次和门禁报告均绑定当前玩家 ZIP SHA-256 `c48a09d1e9df84170f00d24c39a58c37b4ec59a58e277bfb1cb2bf3862f0b358`。
- `verify_release_readiness.ps1` 仍为 `ready_for_external_validation`（失败 0、警告 5）；`verify_publish_environment.ps1` 为 `waiting_for_channel_setup`（失败 0、警告 4）；butler `v15.29.0` 可运行，当前缺口是目标、登录/推送、真实公开 URL 和外部验收证据。
- `verify_public_release_go.ps1 -AllowNoGo` 仍为 `no_go_external_gates_required`（失败 6）：真实 itch 目标、butler 推送、真实公开下载、1 份当前包干净 Windows 首单报告和 3 份当前包外部试玩报告；这些项目不得用本机模拟或样例报告替代。
- 当前交接包 SHA-256 以 `GodotVersion/build/publisher-handoff/PCBuildingLife-0.1.0-dev-publisher-handoff.zip.sha256` 和 `publisher-handoff-audit-0.1.0-dev.json` 为准，避免交接包内文档形成自引用哈希漂移。
- 下一步：取得真实 itch 项目目标后运行 `verify_itch_upload_config.ps1 -RequireTarget`、`prepare_itch_upload.ps1 -Push`，再用公开 URL、真实干净 Windows 和外部试玩报告完成最终 go/no-go。

R5 外部发布轮次自动化与上传假阳性修复（2026-07-27）：

- 修复 `prepare_itch_upload.ps1 -Push` 的成功判定：staging manifest 会先记录 `push_requested=true, pushed=false`，butler 返回退出码 0 后才更新为 `pushed=true`；失败退出码会写入 `butler_exit_code` 并让脚本失败，避免网络、登录或目标错误被当成已上传。
- 新增 `verify_itch_push_failure.ps1`，通过完全本地的假 butler 注入退出码 23；测试确认调用返回非零、失败 manifest 保持 `pushed=false` 并记录退出码，随后自动恢复正常 placeholder staging，全程不访问真实账号。
- 新增 `advance_external_release.ps1`，默认自动检查当前 ZIP、itch 配置和隐藏 channel，处理干净机/试玩回件，刷新 publish environment、release readiness 和最终公开门禁；只有显式 `-Push` 且真实目标、隐藏 channel、butler 都通过时才执行外部上传，`-RequireGo` 用于最终硬门禁。
- 新增 `verify_external_release_cycle.ps1`，验证默认轮次 `push_requested=false / push_performed=false`，并验证缺少真实目标时显式 `-Push` 返回失败且不执行上传；当前输出 `waiting_for_external_evidence`，公开门禁仍为 `no_go_external_gates_required`（6 项真实外部证据）。
- 发布方交接包新增 `external-release-cycle.json/.md` 和 `itch-push-failure-audit.json` 硬证据，发布执行者可在真实目标配置后使用 `advance_external_release.ps1 -Push`，回收报告后使用 `advance_external_release.ps1 -RequireGo`。
- 本轮仅修改发布自动化和文档，不修改玩家运行时代码、UI、导出设置或竞品素材；玩家 ZIP SHA-256 保持 `c48a09d1e9df84170f00d24c39a58c37b4ec59a58e277bfb1cb2bf3862f0b358`。

R5 当前候选包硬门禁 no-go 复核（2026-07-27）：

- 不带 `-AllowNoGo` 运行 `GodotVersion/scripts/verify_public_release_go.ps1`，命令按预期返回退出码 `1`，报告决策为 `no_go_external_gates_required`，失败 6 项：真实 itch 目标、渠道上传、发布环境达到公开下载状态、真实公开下载、干净 Windows 首单报告、当前包外部试玩报告。
- 检查 Windows Sandbox 后确认本机没有可用的 `WindowsSandbox.exe` 或可查询的 `Containers-DisposableClientVM` 功能；没有用本机模拟目录替代真实干净 Windows 证据。
- 当前候选包仍保持可交接但不可扩大公开分发；下一步只接受真实 itch 目标、真实公开 URL、真实干净 Windows 报告和当前 ZIP SHA-256 匹配的外部试玩报告作为门禁输入。

R5 当前外部门禁实时复核（2026-07-30）：

- 当前玩家 ZIP 与同目录 `.sha256` 实测一致，SHA-256 为 `fffea7d5768da81008c28f6346d39d6d157cd06f6011f9434556c5402fd52d07`。
- 默认运行 `advance_external_release.ps1` 成功刷新回件、发布环境、readiness 和最终门禁；结果为 `waiting_for_external_evidence`，`push_requested=false`、`push_performed=false`，没有触发外部上传。
- `verify_release_readiness.ps1` 为 `ready_for_external_validation`（失败 0、警告 5）；不带 `-AllowNoGo` 的 `verify_public_release_go.ps1` 按预期返回退出码 1，仍为 `no_go_external_gates_required`（失败 6）。
- 本机仍无 `itch-upload-config.local.json`，真实干净机回件 0 份、外部试玩回件 0 份；当前只剩真实渠道配置/上传/公开 URL 和外部验证证据，不能用样例报告或本机模拟替代。

R5 发布入口扫描隔离复核（2026-07-31）：

- `GodotVersion/scripts/build_release.ps1` 新增 `Ensure-GodotScanGuards`，在导入项目之前确保 `GodotVersion/build/.gdignore` 与 `GodotVersion/tmp/.gdignore` 存在且为文件；首次在干净工作区执行发布脚本时不会把构建副本、临时截图或发布资料当成 Godot 运行时资源扫描。
- 根目录 `.gitignore` 现在保留两个 `.gdignore`，同时忽略 `GodotVersion/build/*` 与 `GodotVersion/tmp/*` 的生成物；公开玩家包仍不包含这些目录。
- 本轮按当前源码重新运行 `GodotVersion/scripts/build_release.ps1`：28 项 headless 回归、导出 12 单玩家流、首单保存/读取、公开包双 guard、EXE 启动和 ZIP 解压启动均通过。
- 当前候选 ZIP：38,381,329 bytes，SHA-256 `b996902a74abce94b314f1cd0a0efb7efca8d3934d33ba07a8f7055066600028`；EXE SHA-256 `d790f1138d5f6cc0cbf0dbbaa0da53fb0b95b039f10884f55470f2ce4ab4909b`。
- `verify_release_package.ps1` 通过；`verify_ui_visual_screenshots.ps1` 三档截图通过并输出 `warnings=0`，人工抽查主菜单、工作台、订单大厅和 Max Monitor 720p 画面，无重叠、裁切或不可点击区域；`node GodotVersion/scripts/mcp_ui_smoke.mjs` 通过，识别 251 个 UI 文本并推进到“交付订单”。
- 代码、玩家包和本地验证证据一致；真实 itch 目标/上传/公开 URL、1 份干净 Windows 首单报告和 3 份当前包外部试玩报告仍缺失，不以本轮本机证据替代。

R5 当前候选包分发资料复核（2026-07-31）：

- 上传 bundle、itch staging、干净机器验证包、外部试玩 kit、外部验证 round 和发布方交接包均已按当前玩家 ZIP SHA-256 重新生成，并通过各自包内容、哈希和报告一致性审计；权威套件哈希以各自目录的 `.sha256` 文件为准。
- `verify_public_download.ps1`、`verify_publish_environment.ps1`、`verify_release_readiness.ps1`、`verify_external_release_cycle.ps1` 和 `verify_publisher_handoff.ps1` 均已按当前候选包复核；发布轮次确认 `push_requested=false`、`push_performed=false`，缺少真实 itch 目标时显式推送仍返回失败，不会产生上传假阳性。
- 当前本地状态为 `ready_for_external_validation`；最终公开门禁仍为 `no_go_external_gates_required`，真实 itch 目标/上传/公开 URL、1 份干净 Windows 首单报告和 3 份当前包外部试玩报告是唯一剩余发布阻塞。

R5 竞品参考资产边界与 Max Monitor 视觉门禁复核（2026-07-31）：

- 将原 `GodotVersion/assets/pc_creator2` 的 32 张 PNG 与 32 个 `.import` 文件迁移到 `ReferenceAssets/PC_Creator2/legacy_godot_project_copy/`，并逐文件确认与权威参考归档 SHA-256 一致；Godot 目录只保留 `.gdignore` 和 `README.md`。
- 新增 `verify_reference_asset_boundary.ps1` 并接入 `build_release.ps1`，当前结果为 `canonical_pngs=32`、`legacy_pngs=32`、`godot_pointer_files=2`、`runtime_references=0`。
- 当前候选 ZIP：38,381,106 bytes，SHA-256 `05c8a34a8072ef229c161802ecd82cb9a1bcf8d38e6ff561b3d7e1e1acc88c76`；EXE：109,475,744 bytes，SHA-256 `03f6c149ef72e04f0e7d632ad52e28fcf51545c7f8938bf6441cd111123cbdf9`。EXE 哈希变化证明竞品资源集合已从导出资源中移除。
- `verify_monitor_screenshot.gd` 改为支持 `--pcbl-size`，视觉套件对 1280x720、1366x768、1920x1080 分别启动独立进程；每档仍须完成布局断言、PNG 落盘和 `monitor_screenshot=ok`，不会放宽成功标记。
- 完整 `verify_ui_visual_screenshots.ps1` 通过并输出 `warnings=0`；人工检查 1280x720 Max Monitor 与工作台无重叠、裁切或不可点击入口；MCP UI smoke 识别 251 个文本并推进到“交付订单”。
- 上传 bundle、itch staging、干净机器 kit、外部试玩 kit、外部验证 round 和发布方交接包均按上述玩家 ZIP SHA-256 重新生成并审计；真实外部门槛仍未由本机证据替代。

## R6 UI 美术素材增强（2026-08-01）

- 新增 `GodotVersion/assets/ui/icons/` 线性图标素材：工作台、订单、市场、仓库、任务、电源、显示器、检测、交付、存档、读取、重开、系统和关闭。
- 将图标接入首页底部工作流 / 订单 / 系统卡片、市场与仓库入口、任务与系统快捷按钮，保持按钮仍可用键盘和 MCP 操作。
- 通过 1280x720、1366x768、1920x1080 三档 UI 截图检查，`warnings=0`；MCP UI smoke 识别 251 个 UI 文本并到达“交付订单”。
- 竞品素材边界复核通过：`canonical_pngs=32`、`legacy_pngs=32`、`godot_pointer_files=2`、`runtime_references=0`。
- 新候选包：ZIP `dc5a2cec06e35799c8535886f0dd76897f36645e866467defcc2f0e28dd9b73d`；EXE `d72e724a2c635c17ffeb74cd578a21e7b45812565d5b8da487c6ee8a4ec8b0ef`。

## R7 首页流程与订单视觉重构（2026-08-02）

- 新增 `GodotVersion/assets/ui/art/` 本地原创 SVG 素材：客户头像、CPU/GPU/驱动/文件/跑分应用图标，以及铜牌、银牌、金牌交付徽章。
- 首页底部从三张重复信息卡改为紧凑控制台：顶部固定“装机台 / 订单大厅 / 系统中心”模式标签，中部显示当前流程、订单状态、系统状态和任务/交付/监视快捷操作，底部并排显示操作反馈与交付回执。
- 订单大厅继续使用全屏工作区，队列宽度调整为 320px，详情区获得更多空间；订单列表和详情头部使用客户头像，软件配置使用应用图标，派工评估使用交付等级徽章。
- 保存、读取、重开改为图标按钮并补充 tooltip；新增首页模式标签的选中态和键盘/手柄焦点链，保留 MCP 可点击文本和既有节点契约。
- `verify_ui_visual_screenshots.ps1` 增加 Godot 资源自动导入步骤；`verify_home_bottom_dock_screenshot.gd` 与 `verify_order_desk_screenshot.gd` 增加视觉资产存在性断言。
- 三档视觉套件通过：`1280x720`、`1366x768`、`1920x1080`，`warnings=0`；人工抽查首页底部、订单大厅、系统中心和购买反馈截图，无重叠、裁切或不可点击入口。
- MCP UI smoke 通过：`ui_texts=250`，新手引导可见，状态到达“交付订单”，游戏截图成功。
- `build_release.ps1` 通过：核心回归、12 单玩家流程、首单审计、公开包 guard、EXE 启动和 ZIP 解压启动均通过；当前候选 ZIP SHA-256 `fb3ad8d80ca52bdc24866446d8842b2c37f384962287775015c5fb5ae9ddf3b8`，EXE SHA-256 `5679e0f9c03d28ecc2533eb239a459705401cd38a671996c4e4a8dca8872c02b`。
- 当前路线仍为 GitHub 源码仓库推送和 README 上线说明；不创建 GitHub Release，不上传 itch.io。`GodotVersion/build/` 里的 EXE/ZIP 仅作为本机验证产物并保持忽略。

<!-- fixed-validation-commands -->
## 固定验证命令

```powershell
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_data.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_gameplay.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_save_load.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_save_migration.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_release_stability.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_orders.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_order_desk.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_compatibility.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_shop_filters.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_catalog_layout.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_catalog_overlay.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_task_center_overlay.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_onboarding_flow.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_guided_onboarding.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_content_progression.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_order_software_tasks.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_extended_flow.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_cheat_tools.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_workbench_overlay.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_monitor_os.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_software_delivery.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_feedback.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_action_feedback.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_key_animations.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_main_menu.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_pause_menu.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --headless --path GodotVersion -s res://scripts/verify_save_recovery.gd
& GodotVersion\scripts\verify_reference_asset_boundary.ps1
node GodotVersion\scripts\mcp_ui_smoke.mjs
& GodotVersion\scripts\verify_release_package.ps1
& GodotVersion\scripts\prepare_release_upload_bundle.ps1
node GodotVersion\scripts\verify_release_landing_page.mjs
node GodotVersion\scripts\verify_release_landing_page_visual.mjs
& GodotVersion\scripts\verify_public_download.ps1 -ExpectedPackageSha256 '05c8a34a8072ef229c161802ecd82cb9a1bcf8d38e6ff561b3d7e1e1acc88c76'
& GodotVersion\scripts\verify_itch_upload_config.ps1
& GodotVersion\scripts\prepare_itch_upload.ps1 -Hidden
& GodotVersion\scripts\verify_itch_staging.ps1
& GodotVersion\scripts\verify_itch_push_failure.ps1
& GodotVersion\scripts\verify_publish_environment.ps1
& GodotVersion\scripts\prepare_clean_machine_validation_kit.ps1
& GodotVersion\scripts\verify_clean_machine_validation_kit.ps1
& GodotVersion\scripts\receive_clean_machine_reports.ps1
& GodotVersion\scripts\verify_clean_machine_report_intake.ps1
& GodotVersion\scripts\prepare_playtest_kit.ps1
& GodotVersion\scripts\verify_playtest_kit.ps1
& GodotVersion\scripts\receive_playtest_reports.ps1
& GodotVersion\scripts\verify_playtest_report_intake.ps1
& GodotVersion\scripts\summarize_playtest_reports.ps1
& GodotVersion\scripts\prepare_external_validation_round.ps1
& GodotVersion\scripts\verify_external_validation_round.ps1
& GodotVersion\scripts\collect_support_bundle.ps1 -OutputDir GodotVersion\build\support-test
& GodotVersion\scripts\verify_release_readiness.ps1
& GodotVersion\scripts\verify_public_release_go.ps1 -AllowNoGo
& GodotVersion\scripts\verify_external_release_cycle.ps1
& GodotVersion\scripts\prepare_publisher_handoff.ps1
& GodotVersion\scripts\verify_publisher_handoff.ps1
& GodotVersion\scripts\verify_ui_visual_screenshots.ps1
& GodotVersion\scripts\generate_release_contact_sheet.ps1
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_ui_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_home_bottom_dock_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_main_menu_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_pause_menu_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_r2_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_r3_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_catalog_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_catalog_overlay_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_task_center_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_action_feedback_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_key_animation_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_order_desk_screenshot.gd
& 'D:\1exe\3Dev\1GameEngine\Godot_v4.7-rc3_win64\Godot_v4.7-rc3_win64_console.exe' --path GodotVersion -s res://scripts/verify_workbench_screenshot.gd
& GodotVersion\scripts\build_release.ps1
```
