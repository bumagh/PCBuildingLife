# 竞品资源引用与清洗记录

## 本地来源

- PC Creator 2 解包目录：`D:\2work\2AKGStudio\1GameDev\1装机人生\3apk\PC Creator 2\out`
- PC Creator Simulator 解包目录：`D:\2work\2AKGStudio\1GameDev\1装机人生\3apk\PC Creator Simulator\out\Texture2D`
- Godot 边界指针目录：`GodotVersion/assets/pc_creator2`，只允许 `.gdignore` 和 `README.md`
- 权威参考归档目录：`ReferenceAssets/PC_Creator2`
- 历史 Godot 副本归档：`ReferenceAssets/PC_Creator2/legacy_godot_project_copy`

## 当前运行时结论

- 运行时代码已切换到 `GodotVersion/assets/original` 下的自有程序化素材。
- `GodotVersion/assets/pc_creator2` 已清空竞品图片和 `.import` 文件，只保留边界指针，不得被运行时代码 preload，也不得重新放入参考图片。
- `GodotVersion/export_presets.cfg` 已排除 `assets/pc_creator2/*` 和 `assets/pc_creator2/item_icons/*`。
- `verify_reference_asset_boundary.ps1` 会阻止竞品文件重新进入 Godot 工程，并扫描运行时代码中的 `res://assets/pc_creator2` 引用。

## 当前已替换资源

许可边界：本表记录的是复刻参考和临时占位来源，不代表这些素材已经获得公开商用授权。公开上架前必须替换为自研素材、可商用授权素材或取得权利方明确授权。

| 用途 | Godot 路径 | 当前来源 | 状态 |
| --- | --- | --- | --- |
| PC 预览 | `assets/original/workbench/pc_preview.png` | 本项目程序化绘制 | 已替换 |
| CPU 图标 | `assets/original/item_icons/cpu_icon.png` | 本项目程序化绘制 | 已替换 |
| GPU 图标 | `assets/original/item_icons/videocard_icon.png` | 本项目程序化绘制 | 已替换 |
| 主板图标 | `assets/original/item_icons/motherboard_icon.png` | 本项目程序化绘制 | 已替换 |
| 内存图标 | `assets/original/item_icons/ram_icon.png` | 本项目程序化绘制 | 已替换 |
| 电源图标 | `assets/original/item_icons/power_icon.png` | 本项目程序化绘制 | 已替换 |
| 散热器图标 | `assets/original/item_icons/cooler_icon.png` | 本项目程序化绘制 | 已替换 |
| 机箱图标 | `assets/original/item_icons/case_icon.png` | 本项目程序化绘制 | 已替换 |
| HDD 图标 | `assets/original/item_icons/hdd_icon.png` | 本项目程序化绘制 | 已替换 |
| SSD 图标 | `assets/original/item_icons/ssd_icon.png` | 本项目程序化绘制 | 已替换 |
| M.2 图标 | `assets/original/item_icons/m2_icon.png` | 本项目程序化绘制 | 已替换 |
| 配件详情预览 | `assets/original/item_details/*_detail.png` | 本项目程序化绘制 | 已替换 |
| 应用图标 | `assets/images/Logo.png` | 本项目程序化绘制 | 已替换 |

## 已发现问题

- `PC Creator 2/out/sprite/Sprite` 中部分季节皮肤资源存在洋红色花屏，暂不直接引用。
- `SSD.png`、部分 `season_*`、部分 `epic_*` 图在当前解包结果中不可用。
- `Power.png` 文件过小，实际显示效果差，暂不作为电源图标。
- 机箱类资源有些带白底或边缘残留，后续需要做透明化/裁切清洗。

## 后续清洗规则

- 优先使用透明背景、颜色明确、缩到 112x84 仍可识别的图。
- 新增竞品参考只进入 `ReferenceAssets/PC_Creator2`，不得再复制到 Godot 工程目录。
- 每次替换 UI 资产后必须运行截图检查，并人工查看 `GodotVersion-ui-check-1280x720.png`。
- 花屏资源先记录，不进入运行目录；需要时再做重新解包或重绘。

## 主菜单存档工作台复刻记录（2026-07-30）

| 项目 | 结论 |
| --- | --- |
| 竞品行为 | `ReferenceAssets/PC_Creator2/Main.png` 的主界面直接展示等级、资金、房间进度，并提供商店、建造和菜单入口；核心价值是进入游戏时立即看见经营状态和下一操作。 |
| 当前实现 | 旧主菜单右侧只有不可交互网格，继续按钮只表示“有/无存档”，无法预览资金、订单、装机和系统状态。 |
| 差距 | 玩家在继续游戏前看不到当前订单和进度；主存档缺失但 `.bak` 有效时，继续按钮虽然启用，却不能打开恢复流程。 |
| 最小复刻版本 | 主菜单增加可操作的存档工作台，显示资金、当前订单、订单进度、已安装槽位、系统状态和仓库数量，并提供“创建工作台 / 进入当前工作台 / 处理存档”入口。 |
| 验证方式 | `verify_main_menu.gd` 验证有效存档摘要、无存档状态、键盘焦点和仅备份恢复；`verify_main_menu_screenshot.gd` 在 1280x720、1366x768、1920x1080 生成新游戏和继续游戏真实窗口截图；MCP UI smoke 继续推进到交付订单。 |

- 竞品原始 `D:` 解包路径在本轮机器状态下不可用，本轮只读取已归档的 `ReferenceAssets/PC_Creator2/Main.png`。
- 本轮只复刻“状态一眼可见 + 直接操作”的信息架构，没有复制竞品房间图、图标、Logo、文案或像素布局。
- 新主菜单完全由 Godot `Control`、`Container`、颜色和项目数据构成；公开截图 `release/media/01-main-menu.png` 来自当前自有运行时画面。
- `generate_release_contact_sheet.ps1` 会从 9 张当前发布截图重建快速检查图，避免旧竞品或旧 UI 画面混入上传资料。

## 竞品参考资产运行时隔离记录（2026-07-31）

- 权威归档与历史 Godot 副本各包含 32 张 PNG，逐相对路径计算 SHA-256 后完全一致。
- 历史 Godot 副本的 32 个 `.import` 文件一并移入 `ReferenceAssets/PC_Creator2/legacy_godot_project_copy/`，保留审计历史但脱离 Godot 项目扫描范围。
- `GodotVersion/assets/pc_creator2` 当前只含 `.gdignore` 和 `README.md`；运行时引用扫描为 0。
- `build_release.ps1` 在 Godot 导入和导出前强制运行 `verify_reference_asset_boundary.ps1`，边界不满足时发布构建直接失败。
- 当前玩家 ZIP SHA-256 为 `05c8a34a8072ef229c161802ecd82cb9a1bcf8d38e6ff561b3d7e1e1acc88c76`，EXE SHA-256 为 `03f6c149ef72e04f0e7d632ad52e28fcf51545c7f8938bf6441cd111123cbdf9`；公开包继续只使用 `assets/original` 下的自有程序化素材。
